import {
  require_classnames,
  useBootstrapPrefix
} from "./chunk-NKREUT7D.js";
import {
  require_jsx_runtime
} from "./chunk-POO4APME.js";
import {
  __objRest,
  __spreadProps,
  __spreadValues,
  __toModule,
  require_react
} from "./chunk-NQQWALO5.js";

// node_modules/react-bootstrap/esm/Col.js
var import_classnames = __toModule(require_classnames());
var React = __toModule(require_react());
var import_jsx_runtime = __toModule(require_jsx_runtime());
var DEVICE_SIZES = ["xxl", "xl", "lg", "md", "sm", "xs"];
function useCol(_a) {
  var _b = _a, {
    as,
    bsPrefix,
    className
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "col");
  const spans = [];
  const classes = [];
  DEVICE_SIZES.forEach((brkPoint) => {
    const propValue = props[brkPoint];
    delete props[brkPoint];
    let span;
    let offset;
    let order;
    if (typeof propValue === "object" && propValue != null) {
      ({
        span,
        offset,
        order
      } = propValue);
    } else {
      span = propValue;
    }
    const infix = brkPoint !== "xs" ? `-${brkPoint}` : "";
    if (span)
      spans.push(span === true ? `${bsPrefix}${infix}` : `${bsPrefix}${infix}-${span}`);
    if (order != null)
      classes.push(`order${infix}-${order}`);
    if (offset != null)
      classes.push(`offset${infix}-${offset}`);
  });
  return [__spreadProps(__spreadValues({}, props), {
    className: (0, import_classnames.default)(className, ...spans, ...classes)
  }), {
    as,
    bsPrefix,
    spans
  }];
}
var Col = React.forwardRef((props, ref) => {
  const [_a, ..._b] = useCol(props), _c = _a, {
    className
  } = _c, colProps = __objRest(_c, [
    "className"
  ]), [{
    as: Component = "div",
    bsPrefix,
    spans
  }] = _b;
  return (0, import_jsx_runtime.jsx)(Component, __spreadProps(__spreadValues({}, colProps), {
    ref,
    className: (0, import_classnames.default)(className, !spans.length && bsPrefix)
  }));
});
Col.displayName = "Col";
var Col_default = Col;

export {
  useCol,
  Col_default
};
//# sourceMappingURL=chunk-EDGJ5HE6.js.map
