import {
  FloatingLabel_default,
  FormCheckInput_default,
  FormCheck_default,
  FormControl_default,
  FormFloating_default,
  FormGroup_default,
  FormLabel_default,
  FormSelect_default,
  FormText_default,
  Form_default,
  forEach,
  map,
  require_warning
} from "./chunk-H5C3VQ43.js";
import {
  arrow_default,
  computeStyles_default,
  eventListeners_default,
  flip_default,
  hide_default,
  init_arrow,
  init_computeStyles,
  init_createPopper,
  init_enums,
  init_eventListeners,
  init_flip,
  init_hide,
  init_offset,
  init_popperOffsets,
  init_preventOverflow,
  offset_default,
  popperGenerator,
  popperOffsets_default,
  preventOverflow_default
} from "./chunk-XX7H5LZU.js";
import {
  Container_default
} from "./chunk-PUFPDRFA.js";
import {
  AbstractModalHeader_default,
  CloseButton_default,
  Collapse_default,
  ENTERING,
  EXITING,
  Fade_default,
  ModalContext_default,
  Modal_default,
  NavbarBrand_default,
  Navbar_default,
  OffcanvasBody_default,
  OffcanvasHeader_default,
  OffcanvasTitle_default,
  Offcanvas_default,
  TransitionWrapper_default,
  addEventListener_default,
  canUseDOM_default,
  contains,
  createChainedFunction_default,
  divWithClassName_default,
  getSharedManager,
  hasClass,
  listen_default,
  ownerDocument,
  removeEventListener_default,
  safeFindDOMNode,
  transitionEnd,
  transitionEndListener,
  triggerBrowserReflow,
  useWaitForDOMRef,
  useWillUnmount,
  useWindow
} from "./chunk-IXEPSZIP.js";
import {
  require_prop_types
} from "./chunk-Z63GXI7W.js";
import {
  require_react_dom
} from "./chunk-T476DPX7.js";
import {
  Row_default
} from "./chunk-2ZL4DIHT.js";
import {
  Col_default,
  useCol
} from "./chunk-EDGJ5HE6.js";
import {
  Anchor_default,
  Button_default,
  CardHeaderContext_default,
  NavContext_default,
  NavItem_default,
  NavLink_default,
  Nav_default,
  Nav_default2,
  TabContext_default,
  useButtonProps,
  useCallbackRef,
  useEventListener,
  useForceUpdate,
  useIsomorphicEffect_default,
  useNavItem
} from "./chunk-MP462BAL.js";
import {
  NavbarContext_default,
  SelectableContext_default,
  dataAttr,
  makeEventKey,
  qsa,
  require_browser,
  useCommittedRef_default,
  useEventCallback,
  useMergedRefs_default,
  useMounted,
  usePrevious,
  useUncontrolled,
  useUncontrolledProp
} from "./chunk-JBQSRQTY.js";
import {
  createWithBsPrefix
} from "./chunk-5EA65GFZ.js";
import "./chunk-L2PCO7YW.js";
import "./chunk-XHRR7I6C.js";
import {
  ThemeProvider_default,
  require_classnames,
  useBootstrapPrefix,
  useIsRTL
} from "./chunk-NKREUT7D.js";
import {
  require_jsx_runtime
} from "./chunk-POO4APME.js";
import {
  __objRest,
  __spreadProps,
  __spreadValues,
  __toModule,
  require_react
} from "./chunk-NQQWALO5.js";

// node_modules/react-bootstrap/esm/Accordion.js
var import_classnames6 = __toModule(require_classnames());
var React8 = __toModule(require_react());
var import_react5 = __toModule(require_react());

// node_modules/react-bootstrap/esm/AccordionBody.js
var import_classnames2 = __toModule(require_classnames());
var React4 = __toModule(require_react());
var import_react2 = __toModule(require_react());

// node_modules/react-bootstrap/esm/AccordionCollapse.js
var import_classnames = __toModule(require_classnames());
var React2 = __toModule(require_react());
var import_react = __toModule(require_react());

// node_modules/react-bootstrap/esm/AccordionContext.js
var React = __toModule(require_react());
function isAccordionItemSelected(activeEventKey, eventKey) {
  return Array.isArray(activeEventKey) ? activeEventKey.includes(eventKey) : activeEventKey === eventKey;
}
var context = React.createContext({});
context.displayName = "AccordionContext";
var AccordionContext_default = context;

// node_modules/react-bootstrap/esm/AccordionCollapse.js
var import_jsx_runtime = __toModule(require_jsx_runtime());
var AccordionCollapse = React2.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "div",
    bsPrefix,
    className,
    children,
    eventKey
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className",
    "children",
    "eventKey"
  ]);
  const {
    activeEventKey
  } = (0, import_react.useContext)(AccordionContext_default);
  bsPrefix = useBootstrapPrefix(bsPrefix, "accordion-collapse");
  return (0, import_jsx_runtime.jsx)(Collapse_default, __spreadProps(__spreadValues({
    ref,
    in: isAccordionItemSelected(activeEventKey, eventKey)
  }, props), {
    className: (0, import_classnames.default)(className, bsPrefix),
    children: (0, import_jsx_runtime.jsx)(Component2, {
      children: React2.Children.only(children)
    })
  }));
});
AccordionCollapse.displayName = "AccordionCollapse";
var AccordionCollapse_default = AccordionCollapse;

// node_modules/react-bootstrap/esm/AccordionItemContext.js
var React3 = __toModule(require_react());
var context2 = React3.createContext({
  eventKey: ""
});
context2.displayName = "AccordionItemContext";
var AccordionItemContext_default = context2;

// node_modules/react-bootstrap/esm/AccordionBody.js
var import_jsx_runtime2 = __toModule(require_jsx_runtime());
var AccordionBody = React4.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "div",
    bsPrefix,
    className
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "accordion-body");
  const {
    eventKey
  } = (0, import_react2.useContext)(AccordionItemContext_default);
  return (0, import_jsx_runtime2.jsx)(AccordionCollapse_default, {
    eventKey,
    children: (0, import_jsx_runtime2.jsx)(Component2, __spreadProps(__spreadValues({
      ref
    }, props), {
      className: (0, import_classnames2.default)(className, bsPrefix)
    }))
  });
});
AccordionBody.displayName = "AccordionBody";
var AccordionBody_default = AccordionBody;

// node_modules/react-bootstrap/esm/AccordionButton.js
var React5 = __toModule(require_react());
var import_react3 = __toModule(require_react());
var import_classnames3 = __toModule(require_classnames());
var import_jsx_runtime3 = __toModule(require_jsx_runtime());
function useAccordionButton(eventKey, onClick) {
  const {
    activeEventKey,
    onSelect,
    alwaysOpen
  } = (0, import_react3.useContext)(AccordionContext_default);
  return (e) => {
    let eventKeyPassed = eventKey === activeEventKey ? null : eventKey;
    if (alwaysOpen) {
      if (Array.isArray(activeEventKey)) {
        if (activeEventKey.includes(eventKey)) {
          eventKeyPassed = activeEventKey.filter((k) => k !== eventKey);
        } else {
          eventKeyPassed = [...activeEventKey, eventKey];
        }
      } else {
        eventKeyPassed = [eventKey];
      }
    }
    onSelect == null ? void 0 : onSelect(eventKeyPassed, e);
    onClick == null ? void 0 : onClick(e);
  };
}
var AccordionButton = React5.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "button",
    bsPrefix,
    className,
    onClick
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className",
    "onClick"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "accordion-button");
  const {
    eventKey
  } = (0, import_react3.useContext)(AccordionItemContext_default);
  const accordionOnClick = useAccordionButton(eventKey, onClick);
  const {
    activeEventKey
  } = (0, import_react3.useContext)(AccordionContext_default);
  if (Component2 === "button") {
    props.type = "button";
  }
  return (0, import_jsx_runtime3.jsx)(Component2, __spreadProps(__spreadValues({
    ref,
    onClick: accordionOnClick
  }, props), {
    "aria-expanded": eventKey === activeEventKey,
    className: (0, import_classnames3.default)(className, bsPrefix, !isAccordionItemSelected(activeEventKey, eventKey) && "collapsed")
  }));
});
AccordionButton.displayName = "AccordionButton";
var AccordionButton_default = AccordionButton;

// node_modules/react-bootstrap/esm/AccordionHeader.js
var import_classnames4 = __toModule(require_classnames());
var React6 = __toModule(require_react());
var import_jsx_runtime4 = __toModule(require_jsx_runtime());
var AccordionHeader = React6.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "h2",
    bsPrefix,
    className,
    children,
    onClick
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className",
    "children",
    "onClick"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "accordion-header");
  return (0, import_jsx_runtime4.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames4.default)(className, bsPrefix),
    children: (0, import_jsx_runtime4.jsx)(AccordionButton_default, {
      onClick,
      children
    })
  }));
});
AccordionHeader.displayName = "AccordionHeader";
var AccordionHeader_default = AccordionHeader;

// node_modules/react-bootstrap/esm/AccordionItem.js
var import_classnames5 = __toModule(require_classnames());
var React7 = __toModule(require_react());
var import_react4 = __toModule(require_react());
var import_jsx_runtime5 = __toModule(require_jsx_runtime());
var AccordionItem = React7.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "div",
    bsPrefix,
    className,
    eventKey
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className",
    "eventKey"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "accordion-item");
  const contextValue = (0, import_react4.useMemo)(() => ({
    eventKey
  }), [eventKey]);
  return (0, import_jsx_runtime5.jsx)(AccordionItemContext_default.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime5.jsx)(Component2, __spreadProps(__spreadValues({
      ref
    }, props), {
      className: (0, import_classnames5.default)(className, bsPrefix)
    }))
  });
});
AccordionItem.displayName = "AccordionItem";
var AccordionItem_default = AccordionItem;

// node_modules/react-bootstrap/esm/Accordion.js
var import_jsx_runtime6 = __toModule(require_jsx_runtime());
var Accordion = React8.forwardRef((props, ref) => {
  const _a = useUncontrolled(props, {
    activeKey: "onSelect"
  }), {
    as: Component2 = "div",
    activeKey,
    bsPrefix,
    className,
    onSelect,
    flush,
    alwaysOpen
  } = _a, controlledProps = __objRest(_a, [
    "as",
    "activeKey",
    "bsPrefix",
    "className",
    "onSelect",
    "flush",
    "alwaysOpen"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "accordion");
  const contextValue = (0, import_react5.useMemo)(() => ({
    activeEventKey: activeKey,
    onSelect,
    alwaysOpen
  }), [activeKey, onSelect, alwaysOpen]);
  return (0, import_jsx_runtime6.jsx)(AccordionContext_default.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime6.jsx)(Component2, __spreadProps(__spreadValues({
      ref
    }, controlledProps), {
      className: (0, import_classnames6.default)(className, prefix, flush && `${prefix}-flush`)
    }))
  });
});
Accordion.displayName = "Accordion";
var Accordion_default = Object.assign(Accordion, {
  Button: AccordionButton_default,
  Collapse: AccordionCollapse_default,
  Item: AccordionItem_default,
  Header: AccordionHeader_default,
  Body: AccordionBody_default
});

// node_modules/react-bootstrap/esm/Alert.js
var import_classnames7 = __toModule(require_classnames());
var React9 = __toModule(require_react());
var import_jsx_runtime7 = __toModule(require_jsx_runtime());
var import_jsx_runtime8 = __toModule(require_jsx_runtime());
var DivStyledAsH4 = divWithClassName_default("h4");
DivStyledAsH4.displayName = "DivStyledAsH4";
var AlertHeading = createWithBsPrefix("alert-heading", {
  Component: DivStyledAsH4
});
var AlertLink = createWithBsPrefix("alert-link", {
  Component: Anchor_default
});
var defaultProps = {
  variant: "primary",
  show: true,
  transition: Fade_default,
  closeLabel: "Close alert"
};
var Alert = React9.forwardRef((uncontrolledProps, ref) => {
  const _a = useUncontrolled(uncontrolledProps, {
    show: "onClose"
  }), {
    bsPrefix,
    show,
    closeLabel,
    closeVariant,
    className,
    children,
    variant,
    onClose,
    dismissible,
    transition
  } = _a, props = __objRest(_a, [
    "bsPrefix",
    "show",
    "closeLabel",
    "closeVariant",
    "className",
    "children",
    "variant",
    "onClose",
    "dismissible",
    "transition"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "alert");
  const handleClose = useEventCallback((e) => {
    if (onClose) {
      onClose(false, e);
    }
  });
  const Transition = transition === true ? Fade_default : transition;
  const alert = (0, import_jsx_runtime8.jsxs)("div", __spreadProps(__spreadValues({
    role: "alert"
  }, !Transition ? props : void 0), {
    ref,
    className: (0, import_classnames7.default)(className, prefix, variant && `${prefix}-${variant}`, dismissible && `${prefix}-dismissible`),
    children: [dismissible && (0, import_jsx_runtime7.jsx)(CloseButton_default, {
      onClick: handleClose,
      "aria-label": closeLabel,
      variant: closeVariant
    }), children]
  }));
  if (!Transition)
    return show ? alert : null;
  return (0, import_jsx_runtime7.jsx)(Transition, __spreadProps(__spreadValues({
    unmountOnExit: true
  }, props), {
    ref: void 0,
    in: show,
    children: alert
  }));
});
Alert.displayName = "Alert";
Alert.defaultProps = defaultProps;
var Alert_default = Object.assign(Alert, {
  Link: AlertLink,
  Heading: AlertHeading
});

// node_modules/react-bootstrap/esm/Anchor.js
var Anchor_default2 = Anchor_default;

// node_modules/react-bootstrap/esm/Badge.js
var import_classnames8 = __toModule(require_classnames());
var React10 = __toModule(require_react());
var import_jsx_runtime9 = __toModule(require_jsx_runtime());
var defaultProps2 = {
  bg: "primary",
  pill: false
};
var Badge = React10.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    bg,
    pill,
    text,
    className,
    as: Component2 = "span"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "bg",
    "pill",
    "text",
    "className",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "badge");
  return (0, import_jsx_runtime9.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames8.default)(className, prefix, pill && `rounded-pill`, text && `text-${text}`, bg && `bg-${bg}`)
  }));
});
Badge.displayName = "Badge";
Badge.defaultProps = defaultProps2;
var Badge_default = Badge;

// node_modules/react-bootstrap/esm/Breadcrumb.js
var import_classnames10 = __toModule(require_classnames());
var React12 = __toModule(require_react());

// node_modules/react-bootstrap/esm/BreadcrumbItem.js
var import_classnames9 = __toModule(require_classnames());
var React11 = __toModule(require_react());
var import_jsx_runtime10 = __toModule(require_jsx_runtime());
var defaultProps3 = {
  active: false,
  linkProps: {}
};
var BreadcrumbItem = React11.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    active,
    children,
    className,
    as: Component2 = "li",
    linkAs: LinkComponent = Anchor_default,
    linkProps,
    href,
    title,
    target
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "active",
    "children",
    "className",
    "as",
    "linkAs",
    "linkProps",
    "href",
    "title",
    "target"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "breadcrumb-item");
  return (0, import_jsx_runtime10.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames9.default)(prefix, className, {
      active
    }),
    "aria-current": active ? "page" : void 0,
    children: active ? children : (0, import_jsx_runtime10.jsx)(LinkComponent, __spreadProps(__spreadValues({}, linkProps), {
      href,
      title,
      target,
      children
    }))
  }));
});
BreadcrumbItem.displayName = "BreadcrumbItem";
BreadcrumbItem.defaultProps = defaultProps3;
var BreadcrumbItem_default = BreadcrumbItem;

// node_modules/react-bootstrap/esm/Breadcrumb.js
var import_jsx_runtime11 = __toModule(require_jsx_runtime());
var defaultProps4 = {
  label: "breadcrumb",
  listProps: {}
};
var Breadcrumb = React12.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    listProps,
    children,
    label,
    as: Component2 = "nav"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "listProps",
    "children",
    "label",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "breadcrumb");
  return (0, import_jsx_runtime11.jsx)(Component2, __spreadProps(__spreadValues({
    "aria-label": label,
    className,
    ref
  }, props), {
    children: (0, import_jsx_runtime11.jsx)("ol", __spreadProps(__spreadValues({}, listProps), {
      className: (0, import_classnames10.default)(prefix, listProps == null ? void 0 : listProps.className),
      children
    }))
  }));
});
Breadcrumb.displayName = "Breadcrumb";
Breadcrumb.defaultProps = defaultProps4;
var Breadcrumb_default = Object.assign(Breadcrumb, {
  Item: BreadcrumbItem_default
});

// node_modules/react-bootstrap/esm/Button.js
var import_classnames11 = __toModule(require_classnames());
var React13 = __toModule(require_react());
var import_jsx_runtime12 = __toModule(require_jsx_runtime());
var defaultProps5 = {
  variant: "primary",
  active: false,
  disabled: false
};
var Button = React13.forwardRef((_a, ref) => {
  var _b = _a, {
    as,
    bsPrefix,
    variant,
    size: size2,
    active,
    className
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "variant",
    "size",
    "active",
    "className"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "btn");
  const [buttonProps, {
    tagName
  }] = useButtonProps(__spreadValues({
    tagName: as
  }, props));
  const Component2 = tagName;
  return (0, import_jsx_runtime12.jsx)(Component2, __spreadProps(__spreadValues(__spreadValues({}, props), buttonProps), {
    ref,
    className: (0, import_classnames11.default)(className, prefix, active && "active", variant && `${prefix}-${variant}`, size2 && `${prefix}-${size2}`, props.href && props.disabled && "disabled")
  }));
});
Button.displayName = "Button";
Button.defaultProps = defaultProps5;
var Button_default2 = Button;

// node_modules/react-bootstrap/esm/ButtonGroup.js
var import_classnames12 = __toModule(require_classnames());
var React14 = __toModule(require_react());
var import_jsx_runtime13 = __toModule(require_jsx_runtime());
var defaultProps6 = {
  vertical: false,
  role: "group"
};
var ButtonGroup = React14.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    size: size2,
    vertical,
    className,
    as: Component2 = "div"
  } = _b, rest = __objRest(_b, [
    "bsPrefix",
    "size",
    "vertical",
    "className",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "btn-group");
  let baseClass = prefix;
  if (vertical)
    baseClass = `${prefix}-vertical`;
  return (0, import_jsx_runtime13.jsx)(Component2, __spreadProps(__spreadValues({}, rest), {
    ref,
    className: (0, import_classnames12.default)(className, baseClass, size2 && `${prefix}-${size2}`)
  }));
});
ButtonGroup.displayName = "ButtonGroup";
ButtonGroup.defaultProps = defaultProps6;
var ButtonGroup_default = ButtonGroup;

// node_modules/react-bootstrap/esm/ButtonToolbar.js
var import_classnames13 = __toModule(require_classnames());
var React15 = __toModule(require_react());
var import_jsx_runtime14 = __toModule(require_jsx_runtime());
var defaultProps7 = {
  role: "toolbar"
};
var ButtonToolbar = React15.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "btn-toolbar");
  return (0, import_jsx_runtime14.jsx)("div", __spreadProps(__spreadValues({}, props), {
    ref,
    className: (0, import_classnames13.default)(className, prefix)
  }));
});
ButtonToolbar.displayName = "ButtonToolbar";
ButtonToolbar.defaultProps = defaultProps7;
var ButtonToolbar_default = ButtonToolbar;

// node_modules/react-bootstrap/esm/Card.js
var import_classnames16 = __toModule(require_classnames());
var React18 = __toModule(require_react());

// node_modules/react-bootstrap/esm/CardImg.js
var import_classnames14 = __toModule(require_classnames());
var React16 = __toModule(require_react());
var import_jsx_runtime15 = __toModule(require_jsx_runtime());
var CardImg = React16.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    variant,
    as: Component2 = "img"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "variant",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "card-img");
  return (0, import_jsx_runtime15.jsx)(Component2, __spreadValues({
    ref,
    className: (0, import_classnames14.default)(variant ? `${prefix}-${variant}` : prefix, className)
  }, props));
});
CardImg.displayName = "CardImg";
var CardImg_default = CardImg;

// node_modules/react-bootstrap/esm/CardHeader.js
var import_classnames15 = __toModule(require_classnames());
var React17 = __toModule(require_react());
var import_react6 = __toModule(require_react());
var import_jsx_runtime16 = __toModule(require_jsx_runtime());
var CardHeader = React17.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    as: Component2 = "div"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "card-header");
  const contextValue = (0, import_react6.useMemo)(() => ({
    cardHeaderBsPrefix: prefix
  }), [prefix]);
  return (0, import_jsx_runtime16.jsx)(CardHeaderContext_default.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime16.jsx)(Component2, __spreadProps(__spreadValues({
      ref
    }, props), {
      className: (0, import_classnames15.default)(className, prefix)
    }))
  });
});
CardHeader.displayName = "CardHeader";
var CardHeader_default = CardHeader;

// node_modules/react-bootstrap/esm/Card.js
var import_jsx_runtime17 = __toModule(require_jsx_runtime());
var DivStyledAsH5 = divWithClassName_default("h5");
var DivStyledAsH6 = divWithClassName_default("h6");
var CardBody = createWithBsPrefix("card-body");
var CardTitle = createWithBsPrefix("card-title", {
  Component: DivStyledAsH5
});
var CardSubtitle = createWithBsPrefix("card-subtitle", {
  Component: DivStyledAsH6
});
var CardLink = createWithBsPrefix("card-link", {
  Component: "a"
});
var CardText = createWithBsPrefix("card-text", {
  Component: "p"
});
var CardFooter = createWithBsPrefix("card-footer");
var CardImgOverlay = createWithBsPrefix("card-img-overlay");
var defaultProps8 = {
  body: false
};
var Card = React18.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    bg,
    text,
    border,
    body,
    children,
    as: Component2 = "div"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "bg",
    "text",
    "border",
    "body",
    "children",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "card");
  return (0, import_jsx_runtime17.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames16.default)(className, prefix, bg && `bg-${bg}`, text && `text-${text}`, border && `border-${border}`),
    children: body ? (0, import_jsx_runtime17.jsx)(CardBody, {
      children
    }) : children
  }));
});
Card.displayName = "Card";
Card.defaultProps = defaultProps8;
var Card_default = Object.assign(Card, {
  Img: CardImg_default,
  Title: CardTitle,
  Subtitle: CardSubtitle,
  Body: CardBody,
  Link: CardLink,
  Text: CardText,
  Header: CardHeader_default,
  Footer: CardFooter,
  ImgOverlay: CardImgOverlay
});

// node_modules/react-bootstrap/esm/CardGroup.js
var CardGroup_default = createWithBsPrefix("card-group");

// node_modules/@restart/hooks/esm/useUpdateEffect.js
var import_react7 = __toModule(require_react());
function useUpdateEffect(fn, deps) {
  var isFirst = (0, import_react7.useRef)(true);
  (0, import_react7.useEffect)(function() {
    if (isFirst.current) {
      isFirst.current = false;
      return;
    }
    return fn();
  }, deps);
}
var useUpdateEffect_default = useUpdateEffect;

// node_modules/@restart/hooks/esm/useTimeout.js
var import_react8 = __toModule(require_react());
var MAX_DELAY_MS = Math.pow(2, 31) - 1;
function setChainedTimeout(handleRef, fn, timeoutAtMs) {
  var delayMs = timeoutAtMs - Date.now();
  handleRef.current = delayMs <= MAX_DELAY_MS ? setTimeout(fn, delayMs) : setTimeout(function() {
    return setChainedTimeout(handleRef, fn, timeoutAtMs);
  }, MAX_DELAY_MS);
}
function useTimeout() {
  var isMounted = useMounted();
  var handleRef = (0, import_react8.useRef)();
  useWillUnmount(function() {
    return clearTimeout(handleRef.current);
  });
  return (0, import_react8.useMemo)(function() {
    var clear = function clear2() {
      return clearTimeout(handleRef.current);
    };
    function set(fn, delayMs) {
      if (delayMs === void 0) {
        delayMs = 0;
      }
      if (!isMounted())
        return;
      clear();
      if (delayMs <= MAX_DELAY_MS) {
        handleRef.current = setTimeout(fn, delayMs);
      } else {
        setChainedTimeout(handleRef, fn, Date.now() + delayMs);
      }
    }
    return {
      set,
      clear
    };
  }, []);
}

// node_modules/react-bootstrap/esm/Carousel.js
var import_classnames18 = __toModule(require_classnames());
var React20 = __toModule(require_react());
var import_react9 = __toModule(require_react());

// node_modules/react-bootstrap/esm/CarouselCaption.js
var CarouselCaption_default = createWithBsPrefix("carousel-caption");

// node_modules/react-bootstrap/esm/CarouselItem.js
var import_classnames17 = __toModule(require_classnames());
var React19 = __toModule(require_react());
var import_jsx_runtime18 = __toModule(require_jsx_runtime());
var CarouselItem = React19.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "div",
    bsPrefix,
    className
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className"
  ]);
  const finalClassName = (0, import_classnames17.default)(className, useBootstrapPrefix(bsPrefix, "carousel-item"));
  return (0, import_jsx_runtime18.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: finalClassName
  }));
});
CarouselItem.displayName = "CarouselItem";
var CarouselItem_default = CarouselItem;

// node_modules/react-bootstrap/esm/Carousel.js
var import_jsx_runtime19 = __toModule(require_jsx_runtime());
var import_jsx_runtime20 = __toModule(require_jsx_runtime());
var import_jsx_runtime21 = __toModule(require_jsx_runtime());
var SWIPE_THRESHOLD = 40;
var defaultProps9 = {
  slide: true,
  fade: false,
  controls: true,
  indicators: true,
  indicatorLabels: [],
  defaultActiveIndex: 0,
  interval: 5e3,
  keyboard: true,
  pause: "hover",
  wrap: true,
  touch: true,
  prevIcon: (0, import_jsx_runtime19.jsx)("span", {
    "aria-hidden": "true",
    className: "carousel-control-prev-icon"
  }),
  prevLabel: "Previous",
  nextIcon: (0, import_jsx_runtime19.jsx)("span", {
    "aria-hidden": "true",
    className: "carousel-control-next-icon"
  }),
  nextLabel: "Next"
};
function isVisible(element) {
  if (!element || !element.style || !element.parentNode || !element.parentNode.style) {
    return false;
  }
  const elementStyle = getComputedStyle(element);
  return elementStyle.display !== "none" && elementStyle.visibility !== "hidden" && getComputedStyle(element.parentNode).display !== "none";
}
var Carousel = React20.forwardRef((uncontrolledProps, ref) => {
  const _a = useUncontrolled(uncontrolledProps, {
    activeIndex: "onSelect"
  }), {
    as: Component2 = "div",
    bsPrefix,
    slide,
    fade,
    controls,
    indicators,
    indicatorLabels,
    activeIndex,
    onSelect,
    onSlide,
    onSlid,
    interval,
    keyboard,
    onKeyDown,
    pause,
    onMouseOver,
    onMouseOut,
    wrap,
    touch,
    onTouchStart,
    onTouchMove,
    onTouchEnd,
    prevIcon,
    prevLabel,
    nextIcon,
    nextLabel,
    variant,
    className,
    children
  } = _a, props = __objRest(_a, [
    "as",
    "bsPrefix",
    "slide",
    "fade",
    "controls",
    "indicators",
    "indicatorLabels",
    "activeIndex",
    "onSelect",
    "onSlide",
    "onSlid",
    "interval",
    "keyboard",
    "onKeyDown",
    "pause",
    "onMouseOver",
    "onMouseOut",
    "wrap",
    "touch",
    "onTouchStart",
    "onTouchMove",
    "onTouchEnd",
    "prevIcon",
    "prevLabel",
    "nextIcon",
    "nextLabel",
    "variant",
    "className",
    "children"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "carousel");
  const isRTL = useIsRTL();
  const nextDirectionRef = (0, import_react9.useRef)(null);
  const [direction, setDirection] = (0, import_react9.useState)("next");
  const [paused, setPaused] = (0, import_react9.useState)(false);
  const [isSliding, setIsSliding] = (0, import_react9.useState)(false);
  const [renderedActiveIndex, setRenderedActiveIndex] = (0, import_react9.useState)(activeIndex || 0);
  if (!isSliding && activeIndex !== renderedActiveIndex) {
    if (nextDirectionRef.current) {
      setDirection(nextDirectionRef.current);
    } else {
      setDirection((activeIndex || 0) > renderedActiveIndex ? "next" : "prev");
    }
    if (slide) {
      setIsSliding(true);
    }
    setRenderedActiveIndex(activeIndex || 0);
  }
  (0, import_react9.useEffect)(() => {
    if (nextDirectionRef.current) {
      nextDirectionRef.current = null;
    }
  });
  let numChildren = 0;
  let activeChildInterval;
  forEach(children, (child, index) => {
    ++numChildren;
    if (index === activeIndex) {
      activeChildInterval = child.props.interval;
    }
  });
  const activeChildIntervalRef = useCommittedRef_default(activeChildInterval);
  const prev = (0, import_react9.useCallback)((event) => {
    if (isSliding) {
      return;
    }
    let nextActiveIndex = renderedActiveIndex - 1;
    if (nextActiveIndex < 0) {
      if (!wrap) {
        return;
      }
      nextActiveIndex = numChildren - 1;
    }
    nextDirectionRef.current = "prev";
    onSelect == null ? void 0 : onSelect(nextActiveIndex, event);
  }, [isSliding, renderedActiveIndex, onSelect, wrap, numChildren]);
  const next = useEventCallback((event) => {
    if (isSliding) {
      return;
    }
    let nextActiveIndex = renderedActiveIndex + 1;
    if (nextActiveIndex >= numChildren) {
      if (!wrap) {
        return;
      }
      nextActiveIndex = 0;
    }
    nextDirectionRef.current = "next";
    onSelect == null ? void 0 : onSelect(nextActiveIndex, event);
  });
  const elementRef = (0, import_react9.useRef)();
  (0, import_react9.useImperativeHandle)(ref, () => ({
    element: elementRef.current,
    prev,
    next
  }));
  const nextWhenVisible = useEventCallback(() => {
    if (!document.hidden && isVisible(elementRef.current)) {
      if (isRTL) {
        prev();
      } else {
        next();
      }
    }
  });
  const slideDirection = direction === "next" ? "start" : "end";
  useUpdateEffect_default(() => {
    if (slide) {
      return;
    }
    onSlide == null ? void 0 : onSlide(renderedActiveIndex, slideDirection);
    onSlid == null ? void 0 : onSlid(renderedActiveIndex, slideDirection);
  }, [renderedActiveIndex]);
  const orderClassName = `${prefix}-item-${direction}`;
  const directionalClassName = `${prefix}-item-${slideDirection}`;
  const handleEnter = (0, import_react9.useCallback)((node) => {
    triggerBrowserReflow(node);
    onSlide == null ? void 0 : onSlide(renderedActiveIndex, slideDirection);
  }, [onSlide, renderedActiveIndex, slideDirection]);
  const handleEntered = (0, import_react9.useCallback)(() => {
    setIsSliding(false);
    onSlid == null ? void 0 : onSlid(renderedActiveIndex, slideDirection);
  }, [onSlid, renderedActiveIndex, slideDirection]);
  const handleKeyDown = (0, import_react9.useCallback)((event) => {
    if (keyboard && !/input|textarea/i.test(event.target.tagName)) {
      switch (event.key) {
        case "ArrowLeft":
          event.preventDefault();
          if (isRTL) {
            next(event);
          } else {
            prev(event);
          }
          return;
        case "ArrowRight":
          event.preventDefault();
          if (isRTL) {
            prev(event);
          } else {
            next(event);
          }
          return;
        default:
      }
    }
    onKeyDown == null ? void 0 : onKeyDown(event);
  }, [keyboard, onKeyDown, prev, next, isRTL]);
  const handleMouseOver = (0, import_react9.useCallback)((event) => {
    if (pause === "hover") {
      setPaused(true);
    }
    onMouseOver == null ? void 0 : onMouseOver(event);
  }, [pause, onMouseOver]);
  const handleMouseOut = (0, import_react9.useCallback)((event) => {
    setPaused(false);
    onMouseOut == null ? void 0 : onMouseOut(event);
  }, [onMouseOut]);
  const touchStartXRef = (0, import_react9.useRef)(0);
  const touchDeltaXRef = (0, import_react9.useRef)(0);
  const touchUnpauseTimeout = useTimeout();
  const handleTouchStart = (0, import_react9.useCallback)((event) => {
    touchStartXRef.current = event.touches[0].clientX;
    touchDeltaXRef.current = 0;
    if (pause === "hover") {
      setPaused(true);
    }
    onTouchStart == null ? void 0 : onTouchStart(event);
  }, [pause, onTouchStart]);
  const handleTouchMove = (0, import_react9.useCallback)((event) => {
    if (event.touches && event.touches.length > 1) {
      touchDeltaXRef.current = 0;
    } else {
      touchDeltaXRef.current = event.touches[0].clientX - touchStartXRef.current;
    }
    onTouchMove == null ? void 0 : onTouchMove(event);
  }, [onTouchMove]);
  const handleTouchEnd = (0, import_react9.useCallback)((event) => {
    if (touch) {
      const touchDeltaX = touchDeltaXRef.current;
      if (Math.abs(touchDeltaX) > SWIPE_THRESHOLD) {
        if (touchDeltaX > 0) {
          prev(event);
        } else {
          next(event);
        }
      }
    }
    if (pause === "hover") {
      touchUnpauseTimeout.set(() => {
        setPaused(false);
      }, interval || void 0);
    }
    onTouchEnd == null ? void 0 : onTouchEnd(event);
  }, [touch, pause, prev, next, touchUnpauseTimeout, interval, onTouchEnd]);
  const shouldPlay = interval != null && !paused && !isSliding;
  const intervalHandleRef = (0, import_react9.useRef)();
  (0, import_react9.useEffect)(() => {
    var _ref, _activeChildIntervalR;
    if (!shouldPlay) {
      return void 0;
    }
    const nextFunc = isRTL ? prev : next;
    intervalHandleRef.current = window.setInterval(document.visibilityState ? nextWhenVisible : nextFunc, (_ref = (_activeChildIntervalR = activeChildIntervalRef.current) != null ? _activeChildIntervalR : interval) != null ? _ref : void 0);
    return () => {
      if (intervalHandleRef.current !== null) {
        clearInterval(intervalHandleRef.current);
      }
    };
  }, [shouldPlay, prev, next, activeChildIntervalRef, interval, nextWhenVisible, isRTL]);
  const indicatorOnClicks = (0, import_react9.useMemo)(() => indicators && Array.from({
    length: numChildren
  }, (_, index) => (event) => {
    onSelect == null ? void 0 : onSelect(index, event);
  }), [indicators, numChildren, onSelect]);
  return (0, import_jsx_runtime20.jsxs)(Component2, __spreadProps(__spreadValues({
    ref: elementRef
  }, props), {
    onKeyDown: handleKeyDown,
    onMouseOver: handleMouseOver,
    onMouseOut: handleMouseOut,
    onTouchStart: handleTouchStart,
    onTouchMove: handleTouchMove,
    onTouchEnd: handleTouchEnd,
    className: (0, import_classnames18.default)(className, prefix, slide && "slide", fade && `${prefix}-fade`, variant && `${prefix}-${variant}`),
    children: [indicators && (0, import_jsx_runtime19.jsx)("div", {
      className: `${prefix}-indicators`,
      children: map(children, (_, index) => (0, import_jsx_runtime19.jsx)("button", {
        type: "button",
        "data-bs-target": "",
        "aria-label": indicatorLabels != null && indicatorLabels.length ? indicatorLabels[index] : `Slide ${index + 1}`,
        className: index === renderedActiveIndex ? "active" : void 0,
        onClick: indicatorOnClicks ? indicatorOnClicks[index] : void 0,
        "aria-current": index === renderedActiveIndex
      }, index))
    }), (0, import_jsx_runtime19.jsx)("div", {
      className: `${prefix}-inner`,
      children: map(children, (child, index) => {
        const isActive = index === renderedActiveIndex;
        return slide ? (0, import_jsx_runtime19.jsx)(TransitionWrapper_default, {
          in: isActive,
          onEnter: isActive ? handleEnter : void 0,
          onEntered: isActive ? handleEntered : void 0,
          addEndListener: transitionEndListener,
          children: (status, innerProps) => React20.cloneElement(child, __spreadProps(__spreadValues({}, innerProps), {
            className: (0, import_classnames18.default)(child.props.className, isActive && status !== "entered" && orderClassName, (status === "entered" || status === "exiting") && "active", (status === "entering" || status === "exiting") && directionalClassName)
          }))
        }) : React20.cloneElement(child, {
          className: (0, import_classnames18.default)(child.props.className, isActive && "active")
        });
      })
    }), controls && (0, import_jsx_runtime20.jsxs)(import_jsx_runtime21.Fragment, {
      children: [(wrap || activeIndex !== 0) && (0, import_jsx_runtime20.jsxs)(Anchor_default, {
        className: `${prefix}-control-prev`,
        onClick: prev,
        children: [prevIcon, prevLabel && (0, import_jsx_runtime19.jsx)("span", {
          className: "visually-hidden",
          children: prevLabel
        })]
      }), (wrap || activeIndex !== numChildren - 1) && (0, import_jsx_runtime20.jsxs)(Anchor_default, {
        className: `${prefix}-control-next`,
        onClick: next,
        children: [nextIcon, nextLabel && (0, import_jsx_runtime19.jsx)("span", {
          className: "visually-hidden",
          children: nextLabel
        })]
      })]
    })]
  }));
});
Carousel.displayName = "Carousel";
Carousel.defaultProps = defaultProps9;
var Carousel_default = Object.assign(Carousel, {
  Caption: CarouselCaption_default,
  Item: CarouselItem_default
});

// node_modules/react-bootstrap/esm/Dropdown.js
var import_classnames22 = __toModule(require_classnames());
var React31 = __toModule(require_react());
var import_react21 = __toModule(require_react());

// node_modules/@restart/ui/esm/Dropdown.js
var import_react17 = __toModule(require_react());
var React25 = __toModule(require_react());

// node_modules/@restart/ui/esm/DropdownContext.js
var React21 = __toModule(require_react());
var DropdownContext = React21.createContext(null);
var DropdownContext_default = DropdownContext;

// node_modules/@restart/ui/esm/DropdownMenu.js
var import_react13 = __toModule(require_react());
var React22 = __toModule(require_react());

// node_modules/@restart/ui/esm/usePopper.js
var import_react11 = __toModule(require_react());

// node_modules/dequal/dist/index.mjs
var has = Object.prototype.hasOwnProperty;
function find(iter, tar, key) {
  for (key of iter.keys()) {
    if (dequal(key, tar))
      return key;
  }
}
function dequal(foo, bar) {
  var ctor, len, tmp;
  if (foo === bar)
    return true;
  if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
    if (ctor === Date)
      return foo.getTime() === bar.getTime();
    if (ctor === RegExp)
      return foo.toString() === bar.toString();
    if (ctor === Array) {
      if ((len = foo.length) === bar.length) {
        while (len-- && dequal(foo[len], bar[len]))
          ;
      }
      return len === -1;
    }
    if (ctor === Set) {
      if (foo.size !== bar.size) {
        return false;
      }
      for (len of foo) {
        tmp = len;
        if (tmp && typeof tmp === "object") {
          tmp = find(bar, tmp);
          if (!tmp)
            return false;
        }
        if (!bar.has(tmp))
          return false;
      }
      return true;
    }
    if (ctor === Map) {
      if (foo.size !== bar.size) {
        return false;
      }
      for (len of foo) {
        tmp = len[0];
        if (tmp && typeof tmp === "object") {
          tmp = find(bar, tmp);
          if (!tmp)
            return false;
        }
        if (!dequal(len[1], bar.get(tmp))) {
          return false;
        }
      }
      return true;
    }
    if (ctor === ArrayBuffer) {
      foo = new Uint8Array(foo);
      bar = new Uint8Array(bar);
    } else if (ctor === DataView) {
      if ((len = foo.byteLength) === bar.byteLength) {
        while (len-- && foo.getInt8(len) === bar.getInt8(len))
          ;
      }
      return len === -1;
    }
    if (ArrayBuffer.isView(foo)) {
      if ((len = foo.byteLength) === bar.byteLength) {
        while (len-- && foo[len] === bar[len])
          ;
      }
      return len === -1;
    }
    if (!ctor || typeof foo === "object") {
      len = 0;
      for (ctor in foo) {
        if (has.call(foo, ctor) && ++len && !has.call(bar, ctor))
          return false;
        if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor]))
          return false;
      }
      return Object.keys(bar).length === len;
    }
  }
  return foo !== foo && bar !== bar;
}

// node_modules/@restart/hooks/esm/useSafeState.js
var import_react10 = __toModule(require_react());
function useSafeState(state) {
  var isMounted = useMounted();
  return [state[0], (0, import_react10.useCallback)(function(nextState) {
    if (!isMounted())
      return;
    return state[1](nextState);
  }, [isMounted, state[1]])];
}
var useSafeState_default = useSafeState;

// node_modules/@restart/ui/esm/popper.js
init_arrow();
init_computeStyles();
init_eventListeners();
init_flip();
init_hide();
init_offset();
init_popperOffsets();
init_preventOverflow();
init_enums();

// node_modules/@popperjs/core/lib/popper-base.js
init_createPopper();

// node_modules/@restart/ui/esm/popper.js
var createPopper2 = popperGenerator({
  defaultModifiers: [hide_default, popperOffsets_default, computeStyles_default, eventListeners_default, offset_default, flip_default, preventOverflow_default, arrow_default]
});

// node_modules/@restart/ui/esm/usePopper.js
var _excluded = ["enabled", "placement", "strategy", "modifiers"];
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
var disabledApplyStylesModifier = {
  name: "applyStyles",
  enabled: false,
  phase: "afterWrite",
  fn: () => void 0
};
var ariaDescribedByModifier = {
  name: "ariaDescribedBy",
  enabled: true,
  phase: "afterWrite",
  effect: ({
    state
  }) => () => {
    const {
      reference,
      popper
    } = state.elements;
    if ("removeAttribute" in reference) {
      const ids = (reference.getAttribute("aria-describedby") || "").split(",").filter((id) => id.trim() !== popper.id);
      if (!ids.length)
        reference.removeAttribute("aria-describedby");
      else
        reference.setAttribute("aria-describedby", ids.join(","));
    }
  },
  fn: ({
    state
  }) => {
    var _popper$getAttribute;
    const {
      popper,
      reference
    } = state.elements;
    const role = (_popper$getAttribute = popper.getAttribute("role")) == null ? void 0 : _popper$getAttribute.toLowerCase();
    if (popper.id && role === "tooltip" && "setAttribute" in reference) {
      const ids = reference.getAttribute("aria-describedby");
      if (ids && ids.split(",").indexOf(popper.id) !== -1) {
        return;
      }
      reference.setAttribute("aria-describedby", ids ? `${ids},${popper.id}` : popper.id);
    }
  }
};
var EMPTY_MODIFIERS = [];
function usePopper(referenceElement, popperElement, _ref = {}) {
  let {
    enabled = true,
    placement = "bottom",
    strategy = "absolute",
    modifiers = EMPTY_MODIFIERS
  } = _ref, config = _objectWithoutPropertiesLoose(_ref, _excluded);
  const prevModifiers = (0, import_react11.useRef)(modifiers);
  const popperInstanceRef = (0, import_react11.useRef)();
  const update = (0, import_react11.useCallback)(() => {
    var _popperInstanceRef$cu;
    (_popperInstanceRef$cu = popperInstanceRef.current) == null ? void 0 : _popperInstanceRef$cu.update();
  }, []);
  const forceUpdate = (0, import_react11.useCallback)(() => {
    var _popperInstanceRef$cu2;
    (_popperInstanceRef$cu2 = popperInstanceRef.current) == null ? void 0 : _popperInstanceRef$cu2.forceUpdate();
  }, []);
  const [popperState, setState] = useSafeState_default((0, import_react11.useState)({
    placement,
    update,
    forceUpdate,
    attributes: {},
    styles: {
      popper: {},
      arrow: {}
    }
  }));
  const updateModifier = (0, import_react11.useMemo)(() => ({
    name: "updateStateModifier",
    enabled: true,
    phase: "write",
    requires: ["computeStyles"],
    fn: ({
      state
    }) => {
      const styles = {};
      const attributes = {};
      Object.keys(state.elements).forEach((element) => {
        styles[element] = state.styles[element];
        attributes[element] = state.attributes[element];
      });
      setState({
        state,
        styles,
        attributes,
        update,
        forceUpdate,
        placement: state.placement
      });
    }
  }), [update, forceUpdate, setState]);
  const nextModifiers = (0, import_react11.useMemo)(() => {
    if (!dequal(prevModifiers.current, modifiers)) {
      prevModifiers.current = modifiers;
    }
    return prevModifiers.current;
  }, [modifiers]);
  (0, import_react11.useEffect)(() => {
    if (!popperInstanceRef.current || !enabled)
      return;
    popperInstanceRef.current.setOptions({
      placement,
      strategy,
      modifiers: [...nextModifiers, updateModifier, disabledApplyStylesModifier]
    });
  }, [strategy, placement, updateModifier, enabled, nextModifiers]);
  (0, import_react11.useEffect)(() => {
    if (!enabled || referenceElement == null || popperElement == null) {
      return void 0;
    }
    popperInstanceRef.current = createPopper2(referenceElement, popperElement, Object.assign({}, config, {
      placement,
      strategy,
      modifiers: [...nextModifiers, ariaDescribedByModifier, updateModifier]
    }));
    return () => {
      if (popperInstanceRef.current != null) {
        popperInstanceRef.current.destroy();
        popperInstanceRef.current = void 0;
        setState((s) => Object.assign({}, s, {
          attributes: {},
          styles: {
            popper: {}
          }
        }));
      }
    };
  }, [enabled, referenceElement, popperElement]);
  return popperState;
}
var usePopper_default = usePopper;

// node_modules/@restart/ui/esm/useClickOutside.js
var import_react12 = __toModule(require_react());
var import_warning = __toModule(require_warning());
var noop = () => {
};
function isLeftClickEvent(event) {
  return event.button === 0;
}
function isModifiedEvent(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
var getRefTarget = (ref) => ref && ("current" in ref ? ref.current : ref);
function useClickOutside(ref, onClickOutside = noop, {
  disabled,
  clickTrigger = "click"
} = {}) {
  const preventMouseClickOutsideRef = (0, import_react12.useRef)(false);
  const handleMouseCapture = (0, import_react12.useCallback)((e) => {
    const currentTarget = getRefTarget(ref);
    (0, import_warning.default)(!!currentTarget, "ClickOutside captured a close event but does not have a ref to compare it to. useClickOutside(), should be passed a ref that resolves to a DOM node");
    preventMouseClickOutsideRef.current = !currentTarget || isModifiedEvent(e) || !isLeftClickEvent(e) || !!contains(currentTarget, e.target);
  }, [ref]);
  const handleMouse = useEventCallback((e) => {
    if (!preventMouseClickOutsideRef.current) {
      onClickOutside(e);
    }
  });
  (0, import_react12.useEffect)(() => {
    if (disabled || ref == null)
      return void 0;
    const doc = ownerDocument(getRefTarget(ref));
    let currentEvent = (doc.defaultView || window).event;
    const removeMouseCaptureListener = listen_default(doc, clickTrigger, handleMouseCapture, true);
    const removeMouseListener = listen_default(doc, clickTrigger, (e) => {
      if (e === currentEvent) {
        currentEvent = void 0;
        return;
      }
      handleMouse(e);
    });
    let mobileSafariHackListeners = [];
    if ("ontouchstart" in doc.documentElement) {
      mobileSafariHackListeners = [].slice.call(doc.body.children).map((el) => listen_default(el, "mousemove", noop));
    }
    return () => {
      removeMouseCaptureListener();
      removeMouseListener();
      mobileSafariHackListeners.forEach((remove) => remove());
    };
  }, [ref, disabled, clickTrigger, handleMouseCapture, handleMouse]);
}
var useClickOutside_default = useClickOutside;

// node_modules/@restart/ui/esm/mergeOptionsWithPopperConfig.js
function toModifierMap(modifiers) {
  const result = {};
  if (!Array.isArray(modifiers)) {
    return modifiers || result;
  }
  modifiers == null ? void 0 : modifiers.forEach((m) => {
    result[m.name] = m;
  });
  return result;
}
function toModifierArray(map2 = {}) {
  if (Array.isArray(map2))
    return map2;
  return Object.keys(map2).map((k) => {
    map2[k].name = k;
    return map2[k];
  });
}
function mergeOptionsWithPopperConfig({
  enabled,
  enableEvents,
  placement,
  flip,
  offset,
  fixed,
  containerPadding,
  arrowElement,
  popperConfig = {}
}) {
  var _modifiers$preventOve, _modifiers$preventOve2, _modifiers$offset, _modifiers$arrow;
  const modifiers = toModifierMap(popperConfig.modifiers);
  return Object.assign({}, popperConfig, {
    placement,
    enabled,
    strategy: fixed ? "fixed" : popperConfig.strategy,
    modifiers: toModifierArray(Object.assign({}, modifiers, {
      eventListeners: {
        enabled: enableEvents
      },
      preventOverflow: Object.assign({}, modifiers.preventOverflow, {
        options: containerPadding ? Object.assign({
          padding: containerPadding
        }, (_modifiers$preventOve = modifiers.preventOverflow) == null ? void 0 : _modifiers$preventOve.options) : (_modifiers$preventOve2 = modifiers.preventOverflow) == null ? void 0 : _modifiers$preventOve2.options
      }),
      offset: {
        options: Object.assign({
          offset
        }, (_modifiers$offset = modifiers.offset) == null ? void 0 : _modifiers$offset.options)
      },
      arrow: Object.assign({}, modifiers.arrow, {
        enabled: !!arrowElement,
        options: Object.assign({}, (_modifiers$arrow = modifiers.arrow) == null ? void 0 : _modifiers$arrow.options, {
          element: arrowElement
        })
      }),
      flip: Object.assign({
        enabled: !!flip
      }, modifiers.flip)
    }))
  });
}

// node_modules/@restart/ui/esm/DropdownMenu.js
var import_jsx_runtime22 = __toModule(require_jsx_runtime());
var import_jsx_runtime23 = __toModule(require_jsx_runtime());
var _excluded2 = ["children"];
function _objectWithoutPropertiesLoose2(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
var noop2 = () => {
};
function useDropdownMenu(options = {}) {
  const context4 = (0, import_react13.useContext)(DropdownContext_default);
  const [arrowElement, attachArrowRef] = useCallbackRef();
  const hasShownRef = (0, import_react13.useRef)(false);
  const {
    flip,
    offset,
    rootCloseEvent,
    fixed = false,
    placement: placementOverride,
    popperConfig = {},
    enableEventListeners = true,
    usePopper: shouldUsePopper = !!context4
  } = options;
  const show = (context4 == null ? void 0 : context4.show) == null ? !!options.show : context4.show;
  if (show && !hasShownRef.current) {
    hasShownRef.current = true;
  }
  const handleClose = (e) => {
    context4 == null ? void 0 : context4.toggle(false, e);
  };
  const {
    placement,
    setMenu,
    menuElement,
    toggleElement
  } = context4 || {};
  const popper = usePopper_default(toggleElement, menuElement, mergeOptionsWithPopperConfig({
    placement: placementOverride || placement || "bottom-start",
    enabled: shouldUsePopper,
    enableEvents: enableEventListeners == null ? show : enableEventListeners,
    offset,
    flip,
    fixed,
    arrowElement,
    popperConfig
  }));
  const menuProps = Object.assign({
    ref: setMenu || noop2,
    "aria-labelledby": toggleElement == null ? void 0 : toggleElement.id
  }, popper.attributes.popper, {
    style: popper.styles.popper
  });
  const metadata = {
    show,
    placement,
    hasShown: hasShownRef.current,
    toggle: context4 == null ? void 0 : context4.toggle,
    popper: shouldUsePopper ? popper : null,
    arrowProps: shouldUsePopper ? Object.assign({
      ref: attachArrowRef
    }, popper.attributes.arrow, {
      style: popper.styles.arrow
    }) : {}
  };
  useClickOutside_default(menuElement, handleClose, {
    clickTrigger: rootCloseEvent,
    disabled: !show
  });
  return [menuProps, metadata];
}
var defaultProps10 = {
  usePopper: true
};
function DropdownMenu(_ref) {
  let {
    children
  } = _ref, options = _objectWithoutPropertiesLoose2(_ref, _excluded2);
  const [props, meta] = useDropdownMenu(options);
  return (0, import_jsx_runtime23.jsx)(import_jsx_runtime22.Fragment, {
    children: children(props, meta)
  });
}
DropdownMenu.displayName = "DropdownMenu";
DropdownMenu.defaultProps = defaultProps10;
var DropdownMenu_default = DropdownMenu;

// node_modules/@restart/ui/esm/DropdownToggle.js
var import_react15 = __toModule(require_react());
var React23 = __toModule(require_react());

// node_modules/@react-aria/ssr/dist/module.js
var import_react14 = __toModule(require_react());
var $f01a183cc7bdff77849e49ad26eb904$var$defaultContext = {
  prefix: String(Math.round(Math.random() * 1e10)),
  current: 0
};
var $f01a183cc7bdff77849e49ad26eb904$var$SSRContext = import_react14.default.createContext($f01a183cc7bdff77849e49ad26eb904$var$defaultContext);
function SSRProvider(props) {
  let cur = (0, import_react14.useContext)($f01a183cc7bdff77849e49ad26eb904$var$SSRContext);
  let value = (0, import_react14.useMemo)(() => ({
    prefix: cur === $f01a183cc7bdff77849e49ad26eb904$var$defaultContext ? "" : cur.prefix + "-" + ++cur.current,
    current: 0
  }), [cur]);
  return import_react14.default.createElement($f01a183cc7bdff77849e49ad26eb904$var$SSRContext.Provider, {
    value
  }, props.children);
}
var $f01a183cc7bdff77849e49ad26eb904$var$canUseDOM = Boolean(typeof window !== "undefined" && window.document && window.document.createElement);
function useSSRSafeId(defaultId) {
  let ctx = (0, import_react14.useContext)($f01a183cc7bdff77849e49ad26eb904$var$SSRContext);
  if (ctx === $f01a183cc7bdff77849e49ad26eb904$var$defaultContext && !$f01a183cc7bdff77849e49ad26eb904$var$canUseDOM) {
    console.warn("When server rendering, you must wrap your application in an <SSRProvider> to ensure consistent ids are generated between the client and server.");
  }
  return (0, import_react14.useMemo)(() => defaultId || "react-aria" + ctx.prefix + "-" + ++ctx.current, [defaultId]);
}

// node_modules/@restart/ui/esm/DropdownToggle.js
var import_jsx_runtime24 = __toModule(require_jsx_runtime());
var import_jsx_runtime25 = __toModule(require_jsx_runtime());
var isRoleMenu = (el) => {
  var _el$getAttribute;
  return ((_el$getAttribute = el.getAttribute("role")) == null ? void 0 : _el$getAttribute.toLowerCase()) === "menu";
};
var noop3 = () => {
};
function useDropdownToggle() {
  const id = useSSRSafeId();
  const {
    show = false,
    toggle = noop3,
    setToggle,
    menuElement
  } = (0, import_react15.useContext)(DropdownContext_default) || {};
  const handleClick = (0, import_react15.useCallback)((e) => {
    toggle(!show, e);
  }, [show, toggle]);
  const props = {
    id,
    ref: setToggle || noop3,
    onClick: handleClick,
    "aria-expanded": !!show
  };
  if (menuElement && isRoleMenu(menuElement)) {
    props["aria-haspopup"] = true;
  }
  return [props, {
    show,
    toggle
  }];
}
function DropdownToggle({
  children
}) {
  const [props, meta] = useDropdownToggle();
  return (0, import_jsx_runtime25.jsx)(import_jsx_runtime24.Fragment, {
    children: children(props, meta)
  });
}
DropdownToggle.displayName = "DropdownToggle";
var DropdownToggle_default = DropdownToggle;

// node_modules/@restart/ui/esm/DropdownItem.js
var React24 = __toModule(require_react());
var import_react16 = __toModule(require_react());
var import_jsx_runtime26 = __toModule(require_jsx_runtime());
var _excluded3 = ["eventKey", "disabled", "onClick", "active", "as"];
function _objectWithoutPropertiesLoose3(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
function useDropdownItem({
  key,
  href,
  active,
  disabled,
  onClick
}) {
  const onSelectCtx = (0, import_react16.useContext)(SelectableContext_default);
  const navContext = (0, import_react16.useContext)(NavContext_default);
  const {
    activeKey
  } = navContext || {};
  const eventKey = makeEventKey(key, href);
  const isActive = active == null && key != null ? makeEventKey(activeKey) === eventKey : active;
  const handleClick = useEventCallback((event) => {
    if (disabled)
      return;
    onClick == null ? void 0 : onClick(event);
    if (onSelectCtx && !event.isPropagationStopped()) {
      onSelectCtx(eventKey, event);
    }
  });
  return [{
    onClick: handleClick,
    "aria-disabled": disabled || void 0,
    "aria-selected": isActive,
    [dataAttr("dropdown-item")]: ""
  }, {
    isActive
  }];
}
var DropdownItem = React24.forwardRef((_ref, ref) => {
  let {
    eventKey,
    disabled,
    onClick,
    active,
    as: Component2 = Button_default
  } = _ref, props = _objectWithoutPropertiesLoose3(_ref, _excluded3);
  const [dropdownItemProps] = useDropdownItem({
    key: eventKey,
    href: props.href,
    disabled,
    onClick,
    active
  });
  return (0, import_jsx_runtime26.jsx)(Component2, Object.assign({}, props, {
    ref
  }, dropdownItemProps));
});
DropdownItem.displayName = "DropdownItem";
var DropdownItem_default = DropdownItem;

// node_modules/@restart/ui/esm/Dropdown.js
var import_jsx_runtime27 = __toModule(require_jsx_runtime());
function useRefWithUpdate() {
  const forceUpdate = useForceUpdate();
  const ref = (0, import_react17.useRef)(null);
  const attachRef = (0, import_react17.useCallback)((element) => {
    ref.current = element;
    forceUpdate();
  }, [forceUpdate]);
  return [ref, attachRef];
}
function Dropdown({
  defaultShow,
  show: rawShow,
  onSelect,
  onToggle: rawOnToggle,
  itemSelector = `* [${dataAttr("dropdown-item")}]`,
  focusFirstItemOnShow,
  placement = "bottom-start",
  children
}) {
  const window2 = useWindow();
  const [show, onToggle] = useUncontrolledProp(rawShow, defaultShow, rawOnToggle);
  const [menuRef, setMenu] = useRefWithUpdate();
  const menuElement = menuRef.current;
  const [toggleRef, setToggle] = useRefWithUpdate();
  const toggleElement = toggleRef.current;
  const lastShow = usePrevious(show);
  const lastSourceEvent = (0, import_react17.useRef)(null);
  const focusInDropdown = (0, import_react17.useRef)(false);
  const onSelectCtx = (0, import_react17.useContext)(SelectableContext_default);
  const toggle = (0, import_react17.useCallback)((nextShow, event, source = event == null ? void 0 : event.type) => {
    onToggle(nextShow, {
      originalEvent: event,
      source
    });
  }, [onToggle]);
  const handleSelect = useEventCallback((key, event) => {
    onSelect == null ? void 0 : onSelect(key, event);
    toggle(false, event, "select");
    if (!event.isPropagationStopped()) {
      onSelectCtx == null ? void 0 : onSelectCtx(key, event);
    }
  });
  const context4 = (0, import_react17.useMemo)(() => ({
    toggle,
    placement,
    show,
    menuElement,
    toggleElement,
    setMenu,
    setToggle
  }), [toggle, placement, show, menuElement, toggleElement, setMenu, setToggle]);
  if (menuElement && lastShow && !show) {
    focusInDropdown.current = menuElement.contains(menuElement.ownerDocument.activeElement);
  }
  const focusToggle = useEventCallback(() => {
    if (toggleElement && toggleElement.focus) {
      toggleElement.focus();
    }
  });
  const maybeFocusFirst = useEventCallback(() => {
    const type = lastSourceEvent.current;
    let focusType = focusFirstItemOnShow;
    if (focusType == null) {
      focusType = menuRef.current && isRoleMenu(menuRef.current) ? "keyboard" : false;
    }
    if (focusType === false || focusType === "keyboard" && !/^key.+$/.test(type)) {
      return;
    }
    const first = qsa(menuRef.current, itemSelector)[0];
    if (first && first.focus)
      first.focus();
  });
  (0, import_react17.useEffect)(() => {
    if (show)
      maybeFocusFirst();
    else if (focusInDropdown.current) {
      focusInDropdown.current = false;
      focusToggle();
    }
  }, [show, focusInDropdown, focusToggle, maybeFocusFirst]);
  (0, import_react17.useEffect)(() => {
    lastSourceEvent.current = null;
  });
  const getNextFocusedChild = (current, offset) => {
    if (!menuRef.current)
      return null;
    const items = qsa(menuRef.current, itemSelector);
    let index = items.indexOf(current) + offset;
    index = Math.max(0, Math.min(index, items.length));
    return items[index];
  };
  useEventListener((0, import_react17.useCallback)(() => window2.document, [window2]), "keydown", (event) => {
    var _menuRef$current, _toggleRef$current;
    const {
      key
    } = event;
    const target = event.target;
    const fromMenu = (_menuRef$current = menuRef.current) == null ? void 0 : _menuRef$current.contains(target);
    const fromToggle = (_toggleRef$current = toggleRef.current) == null ? void 0 : _toggleRef$current.contains(target);
    const isInput = /input|textarea/i.test(target.tagName);
    if (isInput && (key === " " || key !== "Escape" && fromMenu || key === "Escape" && target.type === "search")) {
      return;
    }
    if (!fromMenu && !fromToggle) {
      return;
    }
    if (key === "Tab" && (!menuRef.current || !show)) {
      return;
    }
    lastSourceEvent.current = event.type;
    const meta = {
      originalEvent: event,
      source: event.type
    };
    switch (key) {
      case "ArrowUp": {
        const next = getNextFocusedChild(target, -1);
        if (next && next.focus)
          next.focus();
        event.preventDefault();
        return;
      }
      case "ArrowDown":
        event.preventDefault();
        if (!show) {
          onToggle(true, meta);
        } else {
          const next = getNextFocusedChild(target, 1);
          if (next && next.focus)
            next.focus();
        }
        return;
      case "Tab":
        addEventListener_default(target.ownerDocument, "keyup", (e) => {
          var _menuRef$current2;
          if (e.key === "Tab" && !e.target || !((_menuRef$current2 = menuRef.current) != null && _menuRef$current2.contains(e.target))) {
            onToggle(false, meta);
          }
        }, {
          once: true
        });
        break;
      case "Escape":
        if (key === "Escape") {
          event.preventDefault();
          event.stopPropagation();
        }
        onToggle(false, meta);
        break;
      default:
    }
  });
  return (0, import_jsx_runtime27.jsx)(SelectableContext_default.Provider, {
    value: handleSelect,
    children: (0, import_jsx_runtime27.jsx)(DropdownContext_default.Provider, {
      value: context4,
      children
    })
  });
}
Dropdown.displayName = "Dropdown";
Dropdown.Menu = DropdownMenu_default;
Dropdown.Toggle = DropdownToggle_default;
Dropdown.Item = DropdownItem_default;
var Dropdown_default = Dropdown;

// node_modules/react-bootstrap/esm/DropdownContext.js
var React26 = __toModule(require_react());
var DropdownContext2 = React26.createContext({});
DropdownContext2.displayName = "DropdownContext";
var DropdownContext_default2 = DropdownContext2;

// node_modules/react-bootstrap/esm/DropdownItem.js
var import_classnames19 = __toModule(require_classnames());
var React27 = __toModule(require_react());
var import_jsx_runtime28 = __toModule(require_jsx_runtime());
var DropdownItem2 = React27.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    eventKey,
    disabled = false,
    onClick,
    active,
    as: Component2 = Anchor_default
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "eventKey",
    "disabled",
    "onClick",
    "active",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "dropdown-item");
  const [dropdownItemProps, meta] = useDropdownItem({
    key: eventKey,
    href: props.href,
    disabled,
    onClick,
    active
  });
  return (0, import_jsx_runtime28.jsx)(Component2, __spreadProps(__spreadValues(__spreadValues({}, props), dropdownItemProps), {
    ref,
    className: (0, import_classnames19.default)(className, prefix, meta.isActive && "active", disabled && "disabled")
  }));
});
DropdownItem2.displayName = "DropdownItem";
var DropdownItem_default2 = DropdownItem2;

// node_modules/react-bootstrap/esm/DropdownMenu.js
var import_classnames20 = __toModule(require_classnames());
var React29 = __toModule(require_react());
var import_react19 = __toModule(require_react());
var import_warning2 = __toModule(require_warning());

// node_modules/react-bootstrap/esm/InputGroupContext.js
var React28 = __toModule(require_react());
var context3 = React28.createContext(null);
context3.displayName = "InputGroupContext";
var InputGroupContext_default = context3;

// node_modules/react-bootstrap/esm/useWrappedRefWithWarning.js
var import_invariant = __toModule(require_browser());
var import_react18 = __toModule(require_react());
function useWrappedRefWithWarning(ref, componentName) {
  if (false)
    return ref;
  const warningRef = (0, import_react18.useCallback)((refValue) => {
    !(refValue == null || !refValue.isReactComponent) ? true ? (0, import_invariant.default)(false, `${componentName} injected a ref to a provided \`as\` component that resolved to a component instance instead of a DOM element. Use \`React.forwardRef\` to provide the injected ref to the class component as a prop in order to pass it directly to a DOM element`) : (0, import_invariant.default)(false) : void 0;
  }, [componentName]);
  return useMergedRefs_default(warningRef, ref);
}

// node_modules/react-bootstrap/esm/types.js
var import_prop_types = __toModule(require_prop_types());
var alignDirection = import_prop_types.default.oneOf(["start", "end"]);
var alignPropType = import_prop_types.default.oneOfType([alignDirection, import_prop_types.default.shape({
  sm: alignDirection
}), import_prop_types.default.shape({
  md: alignDirection
}), import_prop_types.default.shape({
  lg: alignDirection
}), import_prop_types.default.shape({
  xl: alignDirection
}), import_prop_types.default.shape({
  xxl: alignDirection
})]);

// node_modules/react-bootstrap/esm/DropdownMenu.js
var import_jsx_runtime29 = __toModule(require_jsx_runtime());
var defaultProps11 = {
  flip: true
};
function getDropdownMenuPlacement(alignEnd, dropDirection, isRTL) {
  const topStart = isRTL ? "top-end" : "top-start";
  const topEnd = isRTL ? "top-start" : "top-end";
  const bottomStart = isRTL ? "bottom-end" : "bottom-start";
  const bottomEnd = isRTL ? "bottom-start" : "bottom-end";
  const leftStart = isRTL ? "right-start" : "left-start";
  const leftEnd = isRTL ? "right-end" : "left-end";
  const rightStart = isRTL ? "left-start" : "right-start";
  const rightEnd = isRTL ? "left-end" : "right-end";
  let placement = alignEnd ? bottomEnd : bottomStart;
  if (dropDirection === "up")
    placement = alignEnd ? topEnd : topStart;
  else if (dropDirection === "end")
    placement = alignEnd ? rightEnd : rightStart;
  else if (dropDirection === "start")
    placement = alignEnd ? leftEnd : leftStart;
  return placement;
}
var DropdownMenu2 = React29.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    align,
    rootCloseEvent,
    flip,
    show: showProps,
    renderOnMount,
    as: Component2 = "div",
    popperConfig,
    variant
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "align",
    "rootCloseEvent",
    "flip",
    "show",
    "renderOnMount",
    "as",
    "popperConfig",
    "variant"
  ]);
  let alignEnd = false;
  const isNavbar = (0, import_react19.useContext)(NavbarContext_default);
  const prefix = useBootstrapPrefix(bsPrefix, "dropdown-menu");
  const {
    align: contextAlign,
    drop,
    isRTL
  } = (0, import_react19.useContext)(DropdownContext_default2);
  align = align || contextAlign;
  const isInputGroup = (0, import_react19.useContext)(InputGroupContext_default);
  const alignClasses = [];
  if (align) {
    if (typeof align === "object") {
      const keys = Object.keys(align);
      true ? (0, import_warning2.default)(keys.length === 1, "There should only be 1 breakpoint when passing an object to `align`") : void 0;
      if (keys.length) {
        const brkPoint = keys[0];
        const direction = align[brkPoint];
        alignEnd = direction === "start";
        alignClasses.push(`${prefix}-${brkPoint}-${direction}`);
      }
    } else if (align === "end") {
      alignEnd = true;
    }
  }
  const placement = getDropdownMenuPlacement(alignEnd, drop, isRTL);
  const [menuProps, {
    hasShown,
    popper,
    show,
    toggle
  }] = useDropdownMenu({
    flip,
    rootCloseEvent,
    show: showProps,
    usePopper: !isNavbar && alignClasses.length === 0,
    offset: [0, 2],
    popperConfig,
    placement
  });
  menuProps.ref = useMergedRefs_default(useWrappedRefWithWarning(ref, "DropdownMenu"), menuProps.ref);
  useIsomorphicEffect_default(() => {
    if (show)
      popper == null ? void 0 : popper.update();
  }, [show]);
  if (!hasShown && !renderOnMount && !isInputGroup)
    return null;
  if (typeof Component2 !== "string") {
    menuProps.show = show;
    menuProps.close = () => toggle == null ? void 0 : toggle(false);
    menuProps.align = align;
  }
  let style = props.style;
  if (popper != null && popper.placement) {
    style = __spreadValues(__spreadValues({}, props.style), menuProps.style);
    props["x-placement"] = popper.placement;
  }
  return (0, import_jsx_runtime29.jsx)(Component2, __spreadProps(__spreadValues(__spreadProps(__spreadValues(__spreadValues({}, props), menuProps), {
    style
  }), (alignClasses.length || isNavbar) && {
    "data-bs-popper": "static"
  }), {
    className: (0, import_classnames20.default)(className, prefix, show && "show", alignEnd && `${prefix}-end`, variant && `${prefix}-${variant}`, ...alignClasses)
  }));
});
DropdownMenu2.displayName = "DropdownMenu";
DropdownMenu2.defaultProps = defaultProps11;
var DropdownMenu_default2 = DropdownMenu2;

// node_modules/react-bootstrap/esm/DropdownToggle.js
var import_classnames21 = __toModule(require_classnames());
var React30 = __toModule(require_react());
var import_react20 = __toModule(require_react());
var import_jsx_runtime30 = __toModule(require_jsx_runtime());
var DropdownToggle2 = React30.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    split,
    className,
    childBsPrefix,
    as: Component2 = Button_default2
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "split",
    "className",
    "childBsPrefix",
    "as"
  ]);
  const prefix = useBootstrapPrefix(bsPrefix, "dropdown-toggle");
  const dropdownContext = (0, import_react20.useContext)(DropdownContext_default);
  const isInputGroup = (0, import_react20.useContext)(InputGroupContext_default);
  if (childBsPrefix !== void 0) {
    props.bsPrefix = childBsPrefix;
  }
  const [toggleProps] = useDropdownToggle();
  toggleProps.ref = useMergedRefs_default(toggleProps.ref, useWrappedRefWithWarning(ref, "DropdownToggle"));
  return (0, import_jsx_runtime30.jsx)(Component2, __spreadValues(__spreadValues({
    className: (0, import_classnames21.default)(className, prefix, split && `${prefix}-split`, !!isInputGroup && (dropdownContext == null ? void 0 : dropdownContext.show) && "show")
  }, toggleProps), props));
});
DropdownToggle2.displayName = "DropdownToggle";
var DropdownToggle_default2 = DropdownToggle2;

// node_modules/react-bootstrap/esm/Dropdown.js
var import_jsx_runtime31 = __toModule(require_jsx_runtime());
var DropdownHeader = createWithBsPrefix("dropdown-header", {
  defaultProps: {
    role: "heading"
  }
});
var DropdownDivider = createWithBsPrefix("dropdown-divider", {
  Component: "hr",
  defaultProps: {
    role: "separator"
  }
});
var DropdownItemText = createWithBsPrefix("dropdown-item-text", {
  Component: "span"
});
var defaultProps12 = {
  navbar: false,
  align: "start",
  autoClose: true
};
var Dropdown2 = React31.forwardRef((pProps, ref) => {
  const _a = useUncontrolled(pProps, {
    show: "onToggle"
  }), {
    bsPrefix,
    drop,
    show,
    className,
    align,
    onSelect,
    onToggle,
    focusFirstItemOnShow,
    as: Component2 = "div",
    navbar: _4,
    autoClose
  } = _a, props = __objRest(_a, [
    "bsPrefix",
    "drop",
    "show",
    "className",
    "align",
    "onSelect",
    "onToggle",
    "focusFirstItemOnShow",
    "as",
    "navbar",
    "autoClose"
  ]);
  const isInputGroup = (0, import_react21.useContext)(InputGroupContext_default);
  const prefix = useBootstrapPrefix(bsPrefix, "dropdown");
  const isRTL = useIsRTL();
  const isClosingPermitted = (source) => {
    if (autoClose === false)
      return source === "click";
    if (autoClose === "inside")
      return source !== "rootClose";
    if (autoClose === "outside")
      return source !== "select";
    return true;
  };
  const handleToggle = useEventCallback((nextShow, meta) => {
    if (meta.originalEvent.currentTarget === document && (meta.source !== "keydown" || meta.originalEvent.key === "Escape"))
      meta.source = "rootClose";
    if (isClosingPermitted(meta.source))
      onToggle == null ? void 0 : onToggle(nextShow, meta);
  });
  const alignEnd = align === "end";
  const placement = getDropdownMenuPlacement(alignEnd, drop, isRTL);
  const contextValue = (0, import_react21.useMemo)(() => ({
    align,
    drop,
    isRTL
  }), [align, drop, isRTL]);
  return (0, import_jsx_runtime31.jsx)(DropdownContext_default2.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime31.jsx)(Dropdown_default, {
      placement,
      show,
      onSelect,
      onToggle: handleToggle,
      focusFirstItemOnShow,
      itemSelector: `.${prefix}-item:not(.disabled):not(:disabled)`,
      children: isInputGroup ? props.children : (0, import_jsx_runtime31.jsx)(Component2, __spreadProps(__spreadValues({}, props), {
        ref,
        className: (0, import_classnames22.default)(className, show && "show", (!drop || drop === "down") && prefix, drop === "up" && "dropup", drop === "end" && "dropend", drop === "start" && "dropstart")
      }))
    })
  });
});
Dropdown2.displayName = "Dropdown";
Dropdown2.defaultProps = defaultProps12;
var Dropdown_default2 = Object.assign(Dropdown2, {
  Toggle: DropdownToggle_default2,
  Menu: DropdownMenu_default2,
  Item: DropdownItem_default2,
  ItemText: DropdownItemText,
  Divider: DropdownDivider,
  Header: DropdownHeader
});

// node_modules/react-bootstrap/esm/DropdownButton.js
var React32 = __toModule(require_react());
var import_prop_types2 = __toModule(require_prop_types());
var import_jsx_runtime32 = __toModule(require_jsx_runtime());
var import_jsx_runtime33 = __toModule(require_jsx_runtime());
var propTypes = {
  id: import_prop_types2.default.string,
  href: import_prop_types2.default.string,
  onClick: import_prop_types2.default.func,
  title: import_prop_types2.default.node.isRequired,
  disabled: import_prop_types2.default.bool,
  align: alignPropType,
  menuRole: import_prop_types2.default.string,
  renderMenuOnMount: import_prop_types2.default.bool,
  rootCloseEvent: import_prop_types2.default.string,
  menuVariant: import_prop_types2.default.oneOf(["dark"]),
  bsPrefix: import_prop_types2.default.string,
  variant: import_prop_types2.default.string,
  size: import_prop_types2.default.string
};
var DropdownButton = React32.forwardRef((_a, ref) => {
  var _b = _a, {
    title,
    children,
    bsPrefix,
    rootCloseEvent,
    variant,
    size: size2,
    menuRole,
    renderMenuOnMount,
    disabled,
    href,
    id,
    menuVariant
  } = _b, props = __objRest(_b, [
    "title",
    "children",
    "bsPrefix",
    "rootCloseEvent",
    "variant",
    "size",
    "menuRole",
    "renderMenuOnMount",
    "disabled",
    "href",
    "id",
    "menuVariant"
  ]);
  return (0, import_jsx_runtime33.jsxs)(Dropdown_default2, __spreadProps(__spreadValues({
    ref
  }, props), {
    children: [(0, import_jsx_runtime32.jsx)(DropdownToggle_default2, {
      id,
      href,
      size: size2,
      variant,
      disabled,
      childBsPrefix: bsPrefix,
      children: title
    }), (0, import_jsx_runtime32.jsx)(DropdownMenu_default2, {
      role: menuRole,
      renderOnMount: renderMenuOnMount,
      rootCloseEvent,
      variant: menuVariant,
      children
    })]
  }));
});
DropdownButton.displayName = "DropdownButton";
DropdownButton.propTypes = propTypes;
var DropdownButton_default = DropdownButton;

// node_modules/react-bootstrap/esm/Image.js
var import_classnames23 = __toModule(require_classnames());
var React33 = __toModule(require_react());
var import_prop_types3 = __toModule(require_prop_types());
var import_jsx_runtime34 = __toModule(require_jsx_runtime());
var propTypes2 = {
  bsPrefix: import_prop_types3.default.string,
  fluid: import_prop_types3.default.bool,
  rounded: import_prop_types3.default.bool,
  roundedCircle: import_prop_types3.default.bool,
  thumbnail: import_prop_types3.default.bool
};
var defaultProps13 = {
  fluid: false,
  rounded: false,
  roundedCircle: false,
  thumbnail: false
};
var Image = React33.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    fluid,
    rounded,
    roundedCircle,
    thumbnail
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "fluid",
    "rounded",
    "roundedCircle",
    "thumbnail"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "img");
  return (0, import_jsx_runtime34.jsx)("img", __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames23.default)(className, fluid && `${bsPrefix}-fluid`, rounded && `rounded`, roundedCircle && `rounded-circle`, thumbnail && `${bsPrefix}-thumbnail`)
  }));
});
Image.displayName = "Image";
Image.defaultProps = defaultProps13;
var Image_default = Image;

// node_modules/react-bootstrap/esm/FigureImage.js
var import_classnames24 = __toModule(require_classnames());
var React34 = __toModule(require_react());
var import_jsx_runtime35 = __toModule(require_jsx_runtime());
var defaultProps14 = {
  fluid: true
};
var FigureImage = React34.forwardRef((_a, ref) => {
  var _b = _a, {
    className
  } = _b, props = __objRest(_b, [
    "className"
  ]);
  return (0, import_jsx_runtime35.jsx)(Image_default, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames24.default)(className, "figure-img")
  }));
});
FigureImage.displayName = "FigureImage";
FigureImage.propTypes = propTypes2;
FigureImage.defaultProps = defaultProps14;
var FigureImage_default = FigureImage;

// node_modules/react-bootstrap/esm/FigureCaption.js
var FigureCaption = createWithBsPrefix("figure-caption", {
  Component: "figcaption"
});
var FigureCaption_default = FigureCaption;

// node_modules/react-bootstrap/esm/Figure.js
var Figure = createWithBsPrefix("figure", {
  Component: "figure"
});
var Figure_default = Object.assign(Figure, {
  Image: FigureImage_default,
  Caption: FigureCaption_default
});

// node_modules/react-bootstrap/esm/InputGroup.js
var import_classnames25 = __toModule(require_classnames());
var React35 = __toModule(require_react());
var import_react22 = __toModule(require_react());
var import_jsx_runtime36 = __toModule(require_jsx_runtime());
var InputGroupText = createWithBsPrefix("input-group-text", {
  Component: "span"
});
var InputGroupCheckbox = (props) => (0, import_jsx_runtime36.jsx)(InputGroupText, {
  children: (0, import_jsx_runtime36.jsx)(FormCheckInput_default, __spreadValues({
    type: "checkbox"
  }, props))
});
var InputGroupRadio = (props) => (0, import_jsx_runtime36.jsx)(InputGroupText, {
  children: (0, import_jsx_runtime36.jsx)(FormCheckInput_default, __spreadValues({
    type: "radio"
  }, props))
});
var InputGroup = React35.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    size: size2,
    hasValidation,
    className,
    as: Component2 = "div"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "size",
    "hasValidation",
    "className",
    "as"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "input-group");
  const contextValue = (0, import_react22.useMemo)(() => ({}), []);
  return (0, import_jsx_runtime36.jsx)(InputGroupContext_default.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime36.jsx)(Component2, __spreadProps(__spreadValues({
      ref
    }, props), {
      className: (0, import_classnames25.default)(className, bsPrefix, size2 && `${bsPrefix}-${size2}`, hasValidation && "has-validation")
    }))
  });
});
InputGroup.displayName = "InputGroup";
var InputGroup_default = Object.assign(InputGroup, {
  Text: InputGroupText,
  Radio: InputGroupRadio,
  Checkbox: InputGroupCheckbox
});

// node_modules/react-bootstrap/esm/ListGroup.js
var import_classnames27 = __toModule(require_classnames());
var React37 = __toModule(require_react());
var import_warning3 = __toModule(require_warning());

// node_modules/react-bootstrap/esm/ListGroupItem.js
var import_classnames26 = __toModule(require_classnames());
var React36 = __toModule(require_react());
var import_jsx_runtime37 = __toModule(require_jsx_runtime());
var ListGroupItem = React36.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    active,
    disabled,
    eventKey,
    className,
    variant,
    action,
    as
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "active",
    "disabled",
    "eventKey",
    "className",
    "variant",
    "action",
    "as"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "list-group-item");
  const [navItemProps, meta] = useNavItem(__spreadValues({
    key: makeEventKey(eventKey, props.href),
    active
  }, props));
  const handleClick = useEventCallback((event) => {
    if (disabled) {
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    navItemProps.onClick(event);
  });
  if (disabled && props.tabIndex === void 0) {
    props.tabIndex = -1;
    props["aria-disabled"] = true;
  }
  const Component2 = as || (action ? props.href ? "a" : "button" : "div");
  return (0, import_jsx_runtime37.jsx)(Component2, __spreadProps(__spreadValues(__spreadValues({
    ref
  }, props), navItemProps), {
    onClick: handleClick,
    className: (0, import_classnames26.default)(className, bsPrefix, meta.isActive && "active", disabled && "disabled", variant && `${bsPrefix}-${variant}`, action && `${bsPrefix}-action`)
  }));
});
ListGroupItem.displayName = "ListGroupItem";
var ListGroupItem_default = ListGroupItem;

// node_modules/react-bootstrap/esm/ListGroup.js
var import_jsx_runtime38 = __toModule(require_jsx_runtime());
var ListGroup = React37.forwardRef((props, ref) => {
  const _a = useUncontrolled(props, {
    activeKey: "onSelect"
  }), {
    className,
    bsPrefix: initialBsPrefix,
    variant,
    horizontal,
    numbered,
    as = "div"
  } = _a, controlledProps = __objRest(_a, [
    "className",
    "bsPrefix",
    "variant",
    "horizontal",
    "numbered",
    "as"
  ]);
  const bsPrefix = useBootstrapPrefix(initialBsPrefix, "list-group");
  let horizontalVariant;
  if (horizontal) {
    horizontalVariant = horizontal === true ? "horizontal" : `horizontal-${horizontal}`;
  }
  true ? (0, import_warning3.default)(!(horizontal && variant === "flush"), '`variant="flush"` and `horizontal` should not be used together.') : void 0;
  return (0, import_jsx_runtime38.jsx)(Nav_default, __spreadProps(__spreadValues({
    ref
  }, controlledProps), {
    as,
    className: (0, import_classnames27.default)(className, bsPrefix, variant && `${bsPrefix}-${variant}`, horizontalVariant && `${bsPrefix}-${horizontalVariant}`, numbered && `${bsPrefix}-numbered`)
  }));
});
ListGroup.displayName = "ListGroup";
var ListGroup_default = Object.assign(ListGroup, {
  Item: ListGroupItem_default
});

// node_modules/react-bootstrap/esm/Modal.js
var import_classnames30 = __toModule(require_classnames());

// node_modules/dom-helpers/esm/scrollbarSize.js
var size;
function scrollbarSize(recalc) {
  if (!size && size !== 0 || recalc) {
    if (canUseDOM_default) {
      var scrollDiv = document.createElement("div");
      scrollDiv.style.position = "absolute";
      scrollDiv.style.top = "-9999px";
      scrollDiv.style.width = "50px";
      scrollDiv.style.height = "50px";
      scrollDiv.style.overflow = "scroll";
      document.body.appendChild(scrollDiv);
      size = scrollDiv.offsetWidth - scrollDiv.clientWidth;
      document.body.removeChild(scrollDiv);
    }
  }
  return size;
}

// node_modules/react-bootstrap/esm/Modal.js
var React40 = __toModule(require_react());
var import_react23 = __toModule(require_react());

// node_modules/react-bootstrap/esm/ModalBody.js
var ModalBody_default = createWithBsPrefix("modal-body");

// node_modules/react-bootstrap/esm/ModalDialog.js
var import_classnames28 = __toModule(require_classnames());
var React38 = __toModule(require_react());
var import_jsx_runtime39 = __toModule(require_jsx_runtime());
var ModalDialog = React38.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    contentClassName,
    centered,
    size: size2,
    fullscreen,
    children,
    scrollable
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "contentClassName",
    "centered",
    "size",
    "fullscreen",
    "children",
    "scrollable"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "modal");
  const dialogClass = `${bsPrefix}-dialog`;
  const fullScreenClass = typeof fullscreen === "string" ? `${bsPrefix}-fullscreen-${fullscreen}` : `${bsPrefix}-fullscreen`;
  return (0, import_jsx_runtime39.jsx)("div", __spreadProps(__spreadValues({}, props), {
    ref,
    className: (0, import_classnames28.default)(dialogClass, className, size2 && `${bsPrefix}-${size2}`, centered && `${dialogClass}-centered`, scrollable && `${dialogClass}-scrollable`, fullscreen && fullScreenClass),
    children: (0, import_jsx_runtime39.jsx)("div", {
      className: (0, import_classnames28.default)(`${bsPrefix}-content`, contentClassName),
      children
    })
  }));
});
ModalDialog.displayName = "ModalDialog";
var ModalDialog_default = ModalDialog;

// node_modules/react-bootstrap/esm/ModalFooter.js
var ModalFooter_default = createWithBsPrefix("modal-footer");

// node_modules/react-bootstrap/esm/ModalHeader.js
var import_classnames29 = __toModule(require_classnames());
var React39 = __toModule(require_react());
var import_jsx_runtime40 = __toModule(require_jsx_runtime());
var defaultProps15 = {
  closeLabel: "Close",
  closeButton: false
};
var ModalHeader = React39.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "modal-header");
  return (0, import_jsx_runtime40.jsx)(AbstractModalHeader_default, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames29.default)(className, bsPrefix)
  }));
});
ModalHeader.displayName = "ModalHeader";
ModalHeader.defaultProps = defaultProps15;
var ModalHeader_default = ModalHeader;

// node_modules/react-bootstrap/esm/ModalTitle.js
var DivStyledAsH42 = divWithClassName_default("h4");
var ModalTitle_default = createWithBsPrefix("modal-title", {
  Component: DivStyledAsH42
});

// node_modules/react-bootstrap/esm/Modal.js
var import_jsx_runtime41 = __toModule(require_jsx_runtime());
var defaultProps16 = {
  show: false,
  backdrop: true,
  keyboard: true,
  autoFocus: true,
  enforceFocus: true,
  restoreFocus: true,
  animation: true,
  dialogAs: ModalDialog_default
};
function DialogTransition(props) {
  return (0, import_jsx_runtime41.jsx)(Fade_default, __spreadProps(__spreadValues({}, props), {
    timeout: null
  }));
}
function BackdropTransition(props) {
  return (0, import_jsx_runtime41.jsx)(Fade_default, __spreadProps(__spreadValues({}, props), {
    timeout: null
  }));
}
var Modal = React40.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    style,
    dialogClassName,
    contentClassName,
    children,
    dialogAs: Dialog,
    "aria-labelledby": ariaLabelledby,
    show,
    animation,
    backdrop,
    keyboard,
    onEscapeKeyDown,
    onShow,
    onHide,
    container,
    autoFocus,
    enforceFocus,
    restoreFocus,
    restoreFocusOptions,
    onEntered,
    onExit,
    onExiting,
    onEnter,
    onEntering,
    onExited,
    backdropClassName,
    manager: propsManager
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "style",
    "dialogClassName",
    "contentClassName",
    "children",
    "dialogAs",
    "aria-labelledby",
    "show",
    "animation",
    "backdrop",
    "keyboard",
    "onEscapeKeyDown",
    "onShow",
    "onHide",
    "container",
    "autoFocus",
    "enforceFocus",
    "restoreFocus",
    "restoreFocusOptions",
    "onEntered",
    "onExit",
    "onExiting",
    "onEnter",
    "onEntering",
    "onExited",
    "backdropClassName",
    "manager"
  ]);
  const [modalStyle, setStyle] = (0, import_react23.useState)({});
  const [animateStaticModal, setAnimateStaticModal] = (0, import_react23.useState)(false);
  const waitingForMouseUpRef = (0, import_react23.useRef)(false);
  const ignoreBackdropClickRef = (0, import_react23.useRef)(false);
  const removeStaticModalAnimationRef = (0, import_react23.useRef)(null);
  const [modal, setModalRef] = useCallbackRef();
  const mergedRef = useMergedRefs_default(ref, setModalRef);
  const handleHide = useEventCallback(onHide);
  const isRTL = useIsRTL();
  bsPrefix = useBootstrapPrefix(bsPrefix, "modal");
  const modalContext = (0, import_react23.useMemo)(() => ({
    onHide: handleHide
  }), [handleHide]);
  function getModalManager() {
    if (propsManager)
      return propsManager;
    return getSharedManager({
      isRTL
    });
  }
  function updateDialogStyle(node) {
    if (!canUseDOM_default)
      return;
    const containerIsOverflowing = getModalManager().getScrollbarWidth() > 0;
    const modalIsOverflowing = node.scrollHeight > ownerDocument(node).documentElement.clientHeight;
    setStyle({
      paddingRight: containerIsOverflowing && !modalIsOverflowing ? scrollbarSize() : void 0,
      paddingLeft: !containerIsOverflowing && modalIsOverflowing ? scrollbarSize() : void 0
    });
  }
  const handleWindowResize = useEventCallback(() => {
    if (modal) {
      updateDialogStyle(modal.dialog);
    }
  });
  useWillUnmount(() => {
    removeEventListener_default(window, "resize", handleWindowResize);
    removeStaticModalAnimationRef.current == null ? void 0 : removeStaticModalAnimationRef.current();
  });
  const handleDialogMouseDown = () => {
    waitingForMouseUpRef.current = true;
  };
  const handleMouseUp = (e) => {
    if (waitingForMouseUpRef.current && modal && e.target === modal.dialog) {
      ignoreBackdropClickRef.current = true;
    }
    waitingForMouseUpRef.current = false;
  };
  const handleStaticModalAnimation = () => {
    setAnimateStaticModal(true);
    removeStaticModalAnimationRef.current = transitionEnd(modal.dialog, () => {
      setAnimateStaticModal(false);
    });
  };
  const handleStaticBackdropClick = (e) => {
    if (e.target !== e.currentTarget) {
      return;
    }
    handleStaticModalAnimation();
  };
  const handleClick = (e) => {
    if (backdrop === "static") {
      handleStaticBackdropClick(e);
      return;
    }
    if (ignoreBackdropClickRef.current || e.target !== e.currentTarget) {
      ignoreBackdropClickRef.current = false;
      return;
    }
    onHide == null ? void 0 : onHide();
  };
  const handleEscapeKeyDown = (e) => {
    if (!keyboard && backdrop === "static") {
      e.preventDefault();
      handleStaticModalAnimation();
    } else if (keyboard && onEscapeKeyDown) {
      onEscapeKeyDown(e);
    }
  };
  const handleEnter = (node, isAppearing) => {
    if (node) {
      node.style.display = "block";
      updateDialogStyle(node);
    }
    onEnter == null ? void 0 : onEnter(node, isAppearing);
  };
  const handleExit = (node) => {
    removeStaticModalAnimationRef.current == null ? void 0 : removeStaticModalAnimationRef.current();
    onExit == null ? void 0 : onExit(node);
  };
  const handleEntering = (node, isAppearing) => {
    onEntering == null ? void 0 : onEntering(node, isAppearing);
    addEventListener_default(window, "resize", handleWindowResize);
  };
  const handleExited = (node) => {
    if (node)
      node.style.display = "";
    onExited == null ? void 0 : onExited(node);
    removeEventListener_default(window, "resize", handleWindowResize);
  };
  const renderBackdrop = (0, import_react23.useCallback)((backdropProps) => (0, import_jsx_runtime41.jsx)("div", __spreadProps(__spreadValues({}, backdropProps), {
    className: (0, import_classnames30.default)(`${bsPrefix}-backdrop`, backdropClassName, !animation && "show")
  })), [animation, backdropClassName, bsPrefix]);
  const baseModalStyle = __spreadValues(__spreadValues({}, style), modalStyle);
  if (!animation) {
    baseModalStyle.display = "block";
  }
  const renderDialog = (dialogProps) => (0, import_jsx_runtime41.jsx)("div", __spreadProps(__spreadValues({
    role: "dialog"
  }, dialogProps), {
    style: baseModalStyle,
    className: (0, import_classnames30.default)(className, bsPrefix, animateStaticModal && `${bsPrefix}-static`),
    onClick: backdrop ? handleClick : void 0,
    onMouseUp: handleMouseUp,
    "aria-labelledby": ariaLabelledby,
    children: (0, import_jsx_runtime41.jsx)(Dialog, __spreadProps(__spreadValues({}, props), {
      onMouseDown: handleDialogMouseDown,
      className: dialogClassName,
      contentClassName,
      children
    }))
  }));
  return (0, import_jsx_runtime41.jsx)(ModalContext_default.Provider, {
    value: modalContext,
    children: (0, import_jsx_runtime41.jsx)(Modal_default, {
      show,
      ref: mergedRef,
      backdrop,
      container,
      keyboard: true,
      autoFocus,
      enforceFocus,
      restoreFocus,
      restoreFocusOptions,
      onEscapeKeyDown: handleEscapeKeyDown,
      onShow,
      onHide,
      onEnter: handleEnter,
      onEntering: handleEntering,
      onEntered,
      onExit: handleExit,
      onExiting,
      onExited: handleExited,
      manager: getModalManager(),
      transition: animation ? DialogTransition : void 0,
      backdropTransition: animation ? BackdropTransition : void 0,
      renderBackdrop,
      renderDialog
    })
  });
});
Modal.displayName = "Modal";
Modal.defaultProps = defaultProps16;
var Modal_default2 = Object.assign(Modal, {
  Body: ModalBody_default,
  Header: ModalHeader_default,
  Title: ModalTitle_default,
  Footer: ModalFooter_default,
  Dialog: ModalDialog_default,
  TRANSITION_DURATION: 300,
  BACKDROP_TRANSITION_DURATION: 150
});

// node_modules/react-bootstrap/esm/NavDropdown.js
var import_classnames31 = __toModule(require_classnames());
var React41 = __toModule(require_react());
var import_jsx_runtime42 = __toModule(require_jsx_runtime());
var import_jsx_runtime43 = __toModule(require_jsx_runtime());
var NavDropdown = React41.forwardRef((_a, ref) => {
  var _b = _a, {
    id,
    title,
    children,
    bsPrefix,
    className,
    rootCloseEvent,
    menuRole,
    disabled,
    active,
    renderMenuOnMount,
    menuVariant
  } = _b, props = __objRest(_b, [
    "id",
    "title",
    "children",
    "bsPrefix",
    "className",
    "rootCloseEvent",
    "menuRole",
    "disabled",
    "active",
    "renderMenuOnMount",
    "menuVariant"
  ]);
  const navItemPrefix = useBootstrapPrefix(void 0, "nav-item");
  return (0, import_jsx_runtime43.jsxs)(Dropdown_default2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames31.default)(className, navItemPrefix),
    children: [(0, import_jsx_runtime42.jsx)(Dropdown_default2.Toggle, {
      id,
      eventKey: null,
      active,
      disabled,
      childBsPrefix: bsPrefix,
      as: NavLink_default,
      children: title
    }), (0, import_jsx_runtime42.jsx)(Dropdown_default2.Menu, {
      role: menuRole,
      renderOnMount: renderMenuOnMount,
      rootCloseEvent,
      variant: menuVariant,
      children
    })]
  }));
});
NavDropdown.displayName = "NavDropdown";
var NavDropdown_default = Object.assign(NavDropdown, {
  Item: Dropdown_default2.Item,
  ItemText: Dropdown_default2.ItemText,
  Divider: Dropdown_default2.Divider,
  Header: Dropdown_default2.Header
});

// node_modules/react-bootstrap/esm/Overlay.js
var React45 = __toModule(require_react());
var import_react27 = __toModule(require_react());
var import_classnames33 = __toModule(require_classnames());

// node_modules/@restart/ui/esm/Overlay.js
var React42 = __toModule(require_react());
var import_react_dom = __toModule(require_react_dom());
var import_react25 = __toModule(require_react());

// node_modules/@restart/ui/esm/useRootClose.js
var import_react24 = __toModule(require_react());
var escapeKeyCode = 27;
var noop4 = () => {
};
function useRootClose(ref, onRootClose, {
  disabled,
  clickTrigger
} = {}) {
  const onClose = onRootClose || noop4;
  useClickOutside_default(ref, onClose, {
    disabled,
    clickTrigger
  });
  const handleKeyUp = useEventCallback((e) => {
    if (e.keyCode === escapeKeyCode) {
      onClose(e);
    }
  });
  (0, import_react24.useEffect)(() => {
    if (disabled || ref == null)
      return void 0;
    const doc = ownerDocument(getRefTarget(ref));
    let currentEvent = (doc.defaultView || window).event;
    const removeKeyupListener = listen_default(doc, "keyup", (e) => {
      if (e === currentEvent) {
        currentEvent = void 0;
        return;
      }
      handleKeyUp(e);
    });
    return () => {
      removeKeyupListener();
    };
  }, [ref, disabled, handleKeyUp]);
}
var useRootClose_default = useRootClose;

// node_modules/@restart/ui/esm/Overlay.js
var import_jsx_runtime44 = __toModule(require_jsx_runtime());
var Overlay = React42.forwardRef((props, outerRef) => {
  const {
    flip,
    offset,
    placement,
    containerPadding,
    popperConfig = {},
    transition: Transition
  } = props;
  const [rootElement, attachRef] = useCallbackRef();
  const [arrowElement, attachArrowRef] = useCallbackRef();
  const mergedRef = useMergedRefs_default(attachRef, outerRef);
  const container = useWaitForDOMRef(props.container);
  const target = useWaitForDOMRef(props.target);
  const [exited, setExited] = (0, import_react25.useState)(!props.show);
  const popper = usePopper_default(target, rootElement, mergeOptionsWithPopperConfig({
    placement,
    enableEvents: !!props.show,
    containerPadding: containerPadding || 5,
    flip,
    offset,
    arrowElement,
    popperConfig
  }));
  if (props.show) {
    if (exited)
      setExited(false);
  } else if (!props.transition && !exited) {
    setExited(true);
  }
  const handleHidden = (...args) => {
    setExited(true);
    if (props.onExited) {
      props.onExited(...args);
    }
  };
  const mountOverlay = props.show || Transition && !exited;
  useRootClose_default(rootElement, props.onHide, {
    disabled: !props.rootClose || props.rootCloseDisabled,
    clickTrigger: props.rootCloseEvent
  });
  if (!mountOverlay) {
    return null;
  }
  let child = props.children(Object.assign({}, popper.attributes.popper, {
    style: popper.styles.popper,
    ref: mergedRef
  }), {
    popper,
    placement,
    show: !!props.show,
    arrowProps: Object.assign({}, popper.attributes.arrow, {
      style: popper.styles.arrow,
      ref: attachArrowRef
    })
  });
  if (Transition) {
    const {
      onExit,
      onExiting,
      onEnter,
      onEntering,
      onEntered
    } = props;
    child = (0, import_jsx_runtime44.jsx)(Transition, {
      in: props.show,
      appear: true,
      onExit,
      onExiting,
      onExited: handleHidden,
      onEnter,
      onEntering,
      onEntered,
      children: child
    });
  }
  return container ? import_react_dom.default.createPortal(child, container) : null;
});
Overlay.displayName = "Overlay";
var Overlay_default = Overlay;

// node_modules/react-bootstrap/esm/useOverlayOffset.js
var import_react26 = __toModule(require_react());

// node_modules/react-bootstrap/esm/Popover.js
var import_classnames32 = __toModule(require_classnames());
var React44 = __toModule(require_react());

// node_modules/react-bootstrap/esm/PopoverHeader.js
var PopoverHeader_default = createWithBsPrefix("popover-header");

// node_modules/react-bootstrap/esm/PopoverBody.js
var PopoverBody_default = createWithBsPrefix("popover-body");

// node_modules/react-bootstrap/esm/helpers.js
var React43 = __toModule(require_react());
function getOverlayDirection(placement, isRTL) {
  let bsDirection = placement;
  if (placement === "left") {
    bsDirection = isRTL ? "end" : "start";
  } else if (placement === "right") {
    bsDirection = isRTL ? "start" : "end";
  }
  return bsDirection;
}

// node_modules/react-bootstrap/esm/Popover.js
var import_jsx_runtime45 = __toModule(require_jsx_runtime());
var import_jsx_runtime46 = __toModule(require_jsx_runtime());
var defaultProps17 = {
  placement: "right"
};
var Popover = React44.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    placement,
    className,
    style,
    children,
    body,
    arrowProps,
    popper: _,
    show: _1
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "placement",
    "className",
    "style",
    "children",
    "body",
    "arrowProps",
    "popper",
    "show"
  ]);
  const decoratedBsPrefix = useBootstrapPrefix(bsPrefix, "popover");
  const isRTL = useIsRTL();
  const [primaryPlacement] = (placement == null ? void 0 : placement.split("-")) || [];
  const bsDirection = getOverlayDirection(primaryPlacement, isRTL);
  return (0, import_jsx_runtime46.jsxs)("div", __spreadProps(__spreadValues({
    ref,
    role: "tooltip",
    style,
    "x-placement": primaryPlacement,
    className: (0, import_classnames32.default)(className, decoratedBsPrefix, primaryPlacement && `bs-popover-${bsDirection}`)
  }, props), {
    children: [(0, import_jsx_runtime45.jsx)("div", __spreadValues({
      className: "popover-arrow"
    }, arrowProps)), body ? (0, import_jsx_runtime45.jsx)(PopoverBody_default, {
      children
    }) : children]
  }));
});
Popover.defaultProps = defaultProps17;
var Popover_default = Object.assign(Popover, {
  Header: PopoverHeader_default,
  Body: PopoverBody_default,
  POPPER_OFFSET: [0, 8]
});

// node_modules/react-bootstrap/esm/useOverlayOffset.js
function useOverlayOffset() {
  const overlayRef = (0, import_react26.useRef)(null);
  const popoverClass = useBootstrapPrefix(void 0, "popover");
  const offset = (0, import_react26.useMemo)(() => ({
    name: "offset",
    options: {
      offset: () => {
        if (overlayRef.current && hasClass(overlayRef.current, popoverClass)) {
          return Popover_default.POPPER_OFFSET;
        }
        return [0, 0];
      }
    }
  }), [popoverClass]);
  return [overlayRef, [offset]];
}

// node_modules/react-bootstrap/esm/Overlay.js
var import_jsx_runtime47 = __toModule(require_jsx_runtime());
var defaultProps18 = {
  transition: Fade_default,
  rootClose: false,
  show: false,
  placement: "top"
};
function wrapRefs(props, arrowProps) {
  const {
    ref
  } = props;
  const {
    ref: aRef
  } = arrowProps;
  props.ref = ref.__wrapped || (ref.__wrapped = (r) => ref(safeFindDOMNode(r)));
  arrowProps.ref = aRef.__wrapped || (aRef.__wrapped = (r) => aRef(safeFindDOMNode(r)));
}
var Overlay2 = React45.forwardRef((_a, outerRef) => {
  var _b = _a, {
    children: overlay,
    transition,
    popperConfig = {}
  } = _b, outerProps = __objRest(_b, [
    "children",
    "transition",
    "popperConfig"
  ]);
  const popperRef = (0, import_react27.useRef)({});
  const [ref, modifiers] = useOverlayOffset();
  const mergedRef = useMergedRefs_default(outerRef, ref);
  const actualTransition = transition === true ? Fade_default : transition || void 0;
  return (0, import_jsx_runtime47.jsx)(Overlay_default, __spreadProps(__spreadValues({}, outerProps), {
    ref: mergedRef,
    popperConfig: __spreadProps(__spreadValues({}, popperConfig), {
      modifiers: modifiers.concat(popperConfig.modifiers || [])
    }),
    transition: actualTransition,
    children: (overlayProps, {
      arrowProps,
      popper: popperObj,
      show
    }) => {
      var _popperObj$state, _popperObj$state$modi;
      wrapRefs(overlayProps, arrowProps);
      const updatedPlacement = popperObj == null ? void 0 : popperObj.placement;
      const popper = Object.assign(popperRef.current, {
        state: popperObj == null ? void 0 : popperObj.state,
        scheduleUpdate: popperObj == null ? void 0 : popperObj.update,
        placement: updatedPlacement,
        outOfBoundaries: (popperObj == null ? void 0 : (_popperObj$state = popperObj.state) == null ? void 0 : (_popperObj$state$modi = _popperObj$state.modifiersData.hide) == null ? void 0 : _popperObj$state$modi.isReferenceHidden) || false
      });
      if (typeof overlay === "function")
        return overlay(__spreadProps(__spreadValues(__spreadProps(__spreadValues({}, overlayProps), {
          placement: updatedPlacement,
          show
        }), !transition && show && {
          className: "show"
        }), {
          popper,
          arrowProps
        }));
      return React45.cloneElement(overlay, __spreadProps(__spreadValues({}, overlayProps), {
        placement: updatedPlacement,
        arrowProps,
        popper,
        className: (0, import_classnames33.default)(overlay.props.className, !transition && show && "show"),
        style: __spreadValues(__spreadValues({}, overlay.props.style), overlayProps.style)
      }));
    }
  }));
});
Overlay2.displayName = "Overlay";
Overlay2.defaultProps = defaultProps18;
var Overlay_default2 = Overlay2;

// node_modules/react-bootstrap/esm/OverlayTrigger.js
var React46 = __toModule(require_react());
var import_react28 = __toModule(require_react());
var import_warning4 = __toModule(require_warning());
var import_jsx_runtime48 = __toModule(require_jsx_runtime());
var import_jsx_runtime49 = __toModule(require_jsx_runtime());
var import_jsx_runtime50 = __toModule(require_jsx_runtime());
function normalizeDelay(delay) {
  return delay && typeof delay === "object" ? delay : {
    show: delay,
    hide: delay
  };
}
function handleMouseOverOut(handler, args, relatedNative) {
  const [e] = args;
  const target = e.currentTarget;
  const related = e.relatedTarget || e.nativeEvent[relatedNative];
  if ((!related || related !== target) && !contains(target, related)) {
    handler(...args);
  }
}
var defaultProps19 = {
  defaultShow: false,
  trigger: ["hover", "focus"]
};
function OverlayTrigger(_a) {
  var _b = _a, {
    trigger,
    overlay,
    children,
    popperConfig = {},
    show: propsShow,
    defaultShow = false,
    onToggle,
    delay: propsDelay,
    placement,
    flip = placement && placement.indexOf("auto") !== -1
  } = _b, props = __objRest(_b, [
    "trigger",
    "overlay",
    "children",
    "popperConfig",
    "show",
    "defaultShow",
    "onToggle",
    "delay",
    "placement",
    "flip"
  ]);
  const triggerNodeRef = (0, import_react28.useRef)(null);
  const mergedRef = useMergedRefs_default(triggerNodeRef, children.ref);
  const timeout = useTimeout();
  const hoverStateRef = (0, import_react28.useRef)("");
  const [show, setShow] = useUncontrolledProp(propsShow, defaultShow, onToggle);
  const delay = normalizeDelay(propsDelay);
  const {
    onFocus,
    onBlur,
    onClick
  } = typeof children !== "function" ? React46.Children.only(children).props : {};
  const attachRef = (r) => {
    mergedRef(safeFindDOMNode(r));
  };
  const handleShow = (0, import_react28.useCallback)(() => {
    timeout.clear();
    hoverStateRef.current = "show";
    if (!delay.show) {
      setShow(true);
      return;
    }
    timeout.set(() => {
      if (hoverStateRef.current === "show")
        setShow(true);
    }, delay.show);
  }, [delay.show, setShow, timeout]);
  const handleHide = (0, import_react28.useCallback)(() => {
    timeout.clear();
    hoverStateRef.current = "hide";
    if (!delay.hide) {
      setShow(false);
      return;
    }
    timeout.set(() => {
      if (hoverStateRef.current === "hide")
        setShow(false);
    }, delay.hide);
  }, [delay.hide, setShow, timeout]);
  const handleFocus = (0, import_react28.useCallback)((...args) => {
    handleShow();
    onFocus == null ? void 0 : onFocus(...args);
  }, [handleShow, onFocus]);
  const handleBlur = (0, import_react28.useCallback)((...args) => {
    handleHide();
    onBlur == null ? void 0 : onBlur(...args);
  }, [handleHide, onBlur]);
  const handleClick = (0, import_react28.useCallback)((...args) => {
    setShow(!show);
    onClick == null ? void 0 : onClick(...args);
  }, [onClick, setShow, show]);
  const handleMouseOver = (0, import_react28.useCallback)((...args) => {
    handleMouseOverOut(handleShow, args, "fromElement");
  }, [handleShow]);
  const handleMouseOut = (0, import_react28.useCallback)((...args) => {
    handleMouseOverOut(handleHide, args, "toElement");
  }, [handleHide]);
  const triggers = trigger == null ? [] : [].concat(trigger);
  const triggerProps = {
    ref: attachRef
  };
  if (triggers.indexOf("click") !== -1) {
    triggerProps.onClick = handleClick;
  }
  if (triggers.indexOf("focus") !== -1) {
    triggerProps.onFocus = handleFocus;
    triggerProps.onBlur = handleBlur;
  }
  if (triggers.indexOf("hover") !== -1) {
    true ? (0, import_warning4.default)(triggers.length > 1, '[react-bootstrap] Specifying only the `"hover"` trigger limits the visibility of the overlay to just mouse users. Consider also including the `"focus"` trigger so that touch and keyboard only users can see the overlay as well.') : void 0;
    triggerProps.onMouseOver = handleMouseOver;
    triggerProps.onMouseOut = handleMouseOut;
  }
  return (0, import_jsx_runtime50.jsxs)(import_jsx_runtime49.Fragment, {
    children: [typeof children === "function" ? children(triggerProps) : (0, import_react28.cloneElement)(children, triggerProps), (0, import_jsx_runtime48.jsx)(Overlay_default2, __spreadProps(__spreadValues({}, props), {
      show,
      onHide: handleHide,
      flip,
      placement,
      popperConfig,
      target: triggerNodeRef.current,
      children: overlay
    }))]
  });
}
OverlayTrigger.defaultProps = defaultProps19;
var OverlayTrigger_default = OverlayTrigger;

// node_modules/react-bootstrap/esm/PageItem.js
var import_classnames34 = __toModule(require_classnames());
var React47 = __toModule(require_react());
var import_jsx_runtime51 = __toModule(require_jsx_runtime());
var import_jsx_runtime52 = __toModule(require_jsx_runtime());
var defaultProps20 = {
  active: false,
  disabled: false,
  activeLabel: "(current)"
};
var PageItem = React47.forwardRef((_a, ref) => {
  var _b = _a, {
    active,
    disabled,
    className,
    style,
    activeLabel,
    children
  } = _b, props = __objRest(_b, [
    "active",
    "disabled",
    "className",
    "style",
    "activeLabel",
    "children"
  ]);
  const Component2 = active || disabled ? "span" : Anchor_default;
  return (0, import_jsx_runtime51.jsx)("li", {
    ref,
    style,
    className: (0, import_classnames34.default)(className, "page-item", {
      active,
      disabled
    }),
    children: (0, import_jsx_runtime52.jsxs)(Component2, __spreadProps(__spreadValues({
      className: "page-link",
      disabled
    }, props), {
      children: [children, active && activeLabel && (0, import_jsx_runtime51.jsx)("span", {
        className: "visually-hidden",
        children: activeLabel
      })]
    }))
  });
});
PageItem.defaultProps = defaultProps20;
PageItem.displayName = "PageItem";
var PageItem_default = PageItem;
function createButton(name, defaultValue, label = name) {
  function Button2(_a) {
    var _b = _a, {
      children
    } = _b, props = __objRest(_b, [
      "children"
    ]);
    return (0, import_jsx_runtime52.jsxs)(PageItem, __spreadProps(__spreadValues({}, props), {
      children: [(0, import_jsx_runtime51.jsx)("span", {
        "aria-hidden": "true",
        children: children || defaultValue
      }), (0, import_jsx_runtime51.jsx)("span", {
        className: "visually-hidden",
        children: label
      })]
    }));
  }
  Button2.displayName = name;
  return Button2;
}
var First = createButton("First", "\xAB");
var Prev = createButton("Prev", "\u2039", "Previous");
var Ellipsis = createButton("Ellipsis", "\u2026", "More");
var Next = createButton("Next", "\u203A");
var Last = createButton("Last", "\xBB");

// node_modules/react-bootstrap/esm/Pagination.js
var import_classnames35 = __toModule(require_classnames());
var React48 = __toModule(require_react());
var import_jsx_runtime53 = __toModule(require_jsx_runtime());
var Pagination = React48.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    size: size2
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "size"
  ]);
  const decoratedBsPrefix = useBootstrapPrefix(bsPrefix, "pagination");
  return (0, import_jsx_runtime53.jsx)("ul", __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames35.default)(className, decoratedBsPrefix, size2 && `${decoratedBsPrefix}-${size2}`)
  }));
});
Pagination.displayName = "Pagination";
var Pagination_default = Object.assign(Pagination, {
  First,
  Prev,
  Ellipsis,
  Item: PageItem_default,
  Next,
  Last
});

// node_modules/react-bootstrap/esm/Placeholder.js
var React50 = __toModule(require_react());

// node_modules/react-bootstrap/esm/usePlaceholder.js
var import_classnames36 = __toModule(require_classnames());
function usePlaceholder(_a) {
  var _b = _a, {
    animation,
    bg,
    bsPrefix,
    size: size2
  } = _b, props = __objRest(_b, [
    "animation",
    "bg",
    "bsPrefix",
    "size"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "placeholder");
  const [_a2] = useCol(props), _b2 = _a2, {
    className
  } = _b2, colProps = __objRest(_b2, [
    "className"
  ]);
  return __spreadProps(__spreadValues({}, colProps), {
    className: (0, import_classnames36.default)(className, animation ? `${bsPrefix}-${animation}` : bsPrefix, size2 && `${bsPrefix}-${size2}`, bg && `bg-${bg}`)
  });
}

// node_modules/react-bootstrap/esm/PlaceholderButton.js
var React49 = __toModule(require_react());
var import_jsx_runtime54 = __toModule(require_jsx_runtime());
var PlaceholderButton = React49.forwardRef((props, ref) => {
  const placeholderProps = usePlaceholder(props);
  return (0, import_jsx_runtime54.jsx)(Button_default2, __spreadProps(__spreadValues({}, placeholderProps), {
    ref,
    disabled: true,
    tabIndex: -1
  }));
});
PlaceholderButton.displayName = "PlaceholderButton";
var PlaceholderButton_default = PlaceholderButton;

// node_modules/react-bootstrap/esm/Placeholder.js
var import_jsx_runtime55 = __toModule(require_jsx_runtime());
var Placeholder = React50.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "span"
  } = _b, props = __objRest(_b, [
    "as"
  ]);
  const placeholderProps = usePlaceholder(props);
  return (0, import_jsx_runtime55.jsx)(Component2, __spreadProps(__spreadValues({}, placeholderProps), {
    ref
  }));
});
Placeholder.displayName = "Placeholder";
var Placeholder_default = Object.assign(Placeholder, {
  Button: PlaceholderButton_default
});

// node_modules/react-bootstrap/esm/ProgressBar.js
var import_classnames37 = __toModule(require_classnames());
var React51 = __toModule(require_react());
var import_react29 = __toModule(require_react());
var import_jsx_runtime56 = __toModule(require_jsx_runtime());
var ROUND_PRECISION = 1e3;
var defaultProps21 = {
  min: 0,
  max: 100,
  animated: false,
  isChild: false,
  visuallyHidden: false,
  striped: false
};
function getPercentage(now, min, max) {
  const percentage = (now - min) / (max - min) * 100;
  return Math.round(percentage * ROUND_PRECISION) / ROUND_PRECISION;
}
function renderProgressBar(_a, ref) {
  var _b = _a, {
    min,
    now,
    max,
    label,
    visuallyHidden,
    striped,
    animated,
    className,
    style,
    variant,
    bsPrefix
  } = _b, props = __objRest(_b, [
    "min",
    "now",
    "max",
    "label",
    "visuallyHidden",
    "striped",
    "animated",
    "className",
    "style",
    "variant",
    "bsPrefix"
  ]);
  return (0, import_jsx_runtime56.jsx)("div", __spreadProps(__spreadValues({
    ref
  }, props), {
    role: "progressbar",
    className: (0, import_classnames37.default)(className, `${bsPrefix}-bar`, {
      [`bg-${variant}`]: variant,
      [`${bsPrefix}-bar-animated`]: animated,
      [`${bsPrefix}-bar-striped`]: animated || striped
    }),
    style: __spreadValues({
      width: `${getPercentage(now, min, max)}%`
    }, style),
    "aria-valuenow": now,
    "aria-valuemin": min,
    "aria-valuemax": max,
    children: visuallyHidden ? (0, import_jsx_runtime56.jsx)("span", {
      className: "visually-hidden",
      children: label
    }) : label
  }));
}
var ProgressBar = React51.forwardRef((_a, ref) => {
  var _b = _a, {
    isChild
  } = _b, props = __objRest(_b, [
    "isChild"
  ]);
  props.bsPrefix = useBootstrapPrefix(props.bsPrefix, "progress");
  if (isChild) {
    return renderProgressBar(props, ref);
  }
  const _a2 = props, {
    min,
    now,
    max,
    label,
    visuallyHidden,
    striped,
    animated,
    bsPrefix,
    variant,
    className,
    children
  } = _a2, wrapperProps = __objRest(_a2, [
    "min",
    "now",
    "max",
    "label",
    "visuallyHidden",
    "striped",
    "animated",
    "bsPrefix",
    "variant",
    "className",
    "children"
  ]);
  return (0, import_jsx_runtime56.jsx)("div", __spreadProps(__spreadValues({
    ref
  }, wrapperProps), {
    className: (0, import_classnames37.default)(className, bsPrefix),
    children: children ? map(children, (child) => (0, import_react29.cloneElement)(child, {
      isChild: true
    })) : renderProgressBar({
      min,
      now,
      max,
      label,
      visuallyHidden,
      striped,
      animated,
      bsPrefix,
      variant
    }, ref)
  }));
});
ProgressBar.displayName = "ProgressBar";
ProgressBar.defaultProps = defaultProps21;
var ProgressBar_default = ProgressBar;

// node_modules/react-bootstrap/esm/Ratio.js
var import_classnames38 = __toModule(require_classnames());
var React52 = __toModule(require_react());
var import_jsx_runtime57 = __toModule(require_jsx_runtime());
var defaultProps22 = {
  aspectRatio: "1x1"
};
function toPercent(num) {
  if (num <= 0 || num > 100)
    return "100%";
  if (num < 1)
    return `${num * 100}%`;
  return `${num}%`;
}
var Ratio = React52.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    children,
    aspectRatio,
    style
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "children",
    "aspectRatio",
    "style"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "ratio");
  const isCustomRatio = typeof aspectRatio === "number";
  return (0, import_jsx_runtime57.jsx)("div", __spreadProps(__spreadValues({
    ref
  }, props), {
    style: __spreadValues(__spreadValues({}, style), isCustomRatio && {
      "--bs-aspect-ratio": toPercent(aspectRatio)
    }),
    className: (0, import_classnames38.default)(bsPrefix, className, !isCustomRatio && `${bsPrefix}-${aspectRatio}`),
    children: React52.Children.only(children)
  }));
});
Ratio.defaultProps = defaultProps22;
var Ratio_default = Ratio;

// node_modules/react-bootstrap/esm/Spinner.js
var import_classnames39 = __toModule(require_classnames());
var React53 = __toModule(require_react());
var import_jsx_runtime58 = __toModule(require_jsx_runtime());
var Spinner = React53.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    variant,
    animation,
    size: size2,
    as: Component2 = "div",
    className
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "variant",
    "animation",
    "size",
    "as",
    "className"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "spinner");
  const bsSpinnerPrefix = `${bsPrefix}-${animation}`;
  return (0, import_jsx_runtime58.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames39.default)(className, bsSpinnerPrefix, size2 && `${bsSpinnerPrefix}-${size2}`, variant && `text-${variant}`)
  }));
});
Spinner.displayName = "Spinner";
var Spinner_default = Spinner;

// node_modules/react-bootstrap/esm/SplitButton.js
var React54 = __toModule(require_react());
var import_prop_types4 = __toModule(require_prop_types());
var import_jsx_runtime59 = __toModule(require_jsx_runtime());
var import_jsx_runtime60 = __toModule(require_jsx_runtime());
var propTypes3 = {
  id: import_prop_types4.default.string,
  toggleLabel: import_prop_types4.default.string,
  href: import_prop_types4.default.string,
  target: import_prop_types4.default.string,
  onClick: import_prop_types4.default.func,
  title: import_prop_types4.default.node.isRequired,
  type: import_prop_types4.default.string,
  disabled: import_prop_types4.default.bool,
  align: alignPropType,
  menuRole: import_prop_types4.default.string,
  renderMenuOnMount: import_prop_types4.default.bool,
  rootCloseEvent: import_prop_types4.default.string,
  bsPrefix: import_prop_types4.default.string,
  variant: import_prop_types4.default.string,
  size: import_prop_types4.default.string
};
var defaultProps23 = {
  toggleLabel: "Toggle dropdown",
  type: "button"
};
var SplitButton = React54.forwardRef((_a, ref) => {
  var _b = _a, {
    id,
    bsPrefix,
    size: size2,
    variant,
    title,
    type,
    toggleLabel,
    children,
    onClick,
    href,
    target,
    menuRole,
    renderMenuOnMount,
    rootCloseEvent
  } = _b, props = __objRest(_b, [
    "id",
    "bsPrefix",
    "size",
    "variant",
    "title",
    "type",
    "toggleLabel",
    "children",
    "onClick",
    "href",
    "target",
    "menuRole",
    "renderMenuOnMount",
    "rootCloseEvent"
  ]);
  return (0, import_jsx_runtime60.jsxs)(Dropdown_default2, __spreadProps(__spreadValues({
    ref
  }, props), {
    as: ButtonGroup_default,
    children: [(0, import_jsx_runtime59.jsx)(Button_default2, {
      size: size2,
      variant,
      disabled: props.disabled,
      bsPrefix,
      href,
      target,
      onClick,
      type,
      children: title
    }), (0, import_jsx_runtime59.jsx)(Dropdown_default2.Toggle, {
      split: true,
      id,
      size: size2,
      variant,
      disabled: props.disabled,
      childBsPrefix: bsPrefix,
      children: (0, import_jsx_runtime59.jsx)("span", {
        className: "visually-hidden",
        children: toggleLabel
      })
    }), (0, import_jsx_runtime59.jsx)(Dropdown_default2.Menu, {
      role: menuRole,
      renderOnMount: renderMenuOnMount,
      rootCloseEvent,
      children
    })]
  }));
});
SplitButton.propTypes = propTypes3;
SplitButton.defaultProps = defaultProps23;
SplitButton.displayName = "SplitButton";
var SplitButton_default = SplitButton;

// node_modules/react-bootstrap/esm/SSRProvider.js
var SSRProvider_default = SSRProvider;

// node_modules/react-bootstrap/esm/Stack.js
var import_classnames40 = __toModule(require_classnames());
var React55 = __toModule(require_react());

// node_modules/react-bootstrap/esm/createUtilityClasses.js
var import_prop_types5 = __toModule(require_prop_types());
var DEVICE_SIZES = ["xxl", "xl", "lg", "md", "sm", "xs"];
function createUtilityClassName(utilityValues) {
  const classes = [];
  Object.entries(utilityValues).forEach(([utilName, utilValue]) => {
    if (utilValue != null) {
      if (typeof utilValue === "object") {
        DEVICE_SIZES.forEach((brkPoint) => {
          const bpValue = utilValue[brkPoint];
          if (bpValue != null) {
            const infix = brkPoint !== "xs" ? `-${brkPoint}` : "";
            classes.push(`${utilName}${infix}-${bpValue}`);
          }
        });
      } else {
        classes.push(`${utilName}-${utilValue}`);
      }
    }
  });
  return classes;
}

// node_modules/react-bootstrap/esm/Stack.js
var import_jsx_runtime61 = __toModule(require_jsx_runtime());
var Stack = React55.forwardRef((_a, ref) => {
  var _b = _a, {
    as: Component2 = "div",
    bsPrefix,
    className,
    direction,
    gap
  } = _b, props = __objRest(_b, [
    "as",
    "bsPrefix",
    "className",
    "direction",
    "gap"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, direction === "horizontal" ? "hstack" : "vstack");
  return (0, import_jsx_runtime61.jsx)(Component2, __spreadProps(__spreadValues({}, props), {
    ref,
    className: (0, import_classnames40.default)(className, bsPrefix, ...createUtilityClassName({
      gap
    }))
  }));
});
Stack.displayName = "Stack";
var Stack_default = Stack;

// node_modules/react-bootstrap/esm/Tab.js
var import_prop_types6 = __toModule(require_prop_types());
var React60 = __toModule(require_react());

// node_modules/react-bootstrap/esm/TabContainer.js
var React58 = __toModule(require_react());

// node_modules/@restart/ui/esm/Tabs.js
var React57 = __toModule(require_react());
var import_react32 = __toModule(require_react());

// node_modules/@restart/ui/esm/TabPanel.js
var React56 = __toModule(require_react());
var import_react31 = __toModule(require_react());

// node_modules/@restart/ui/esm/NoopTransition.js
var import_react30 = __toModule(require_react());
function NoopTransition({
  children,
  in: inProp,
  mountOnEnter,
  unmountOnExit
}) {
  const hasEnteredRef = (0, import_react30.useRef)(inProp);
  (0, import_react30.useEffect)(() => {
    if (inProp)
      hasEnteredRef.current = true;
  }, [inProp]);
  if (inProp)
    return children;
  if (unmountOnExit) {
    return null;
  }
  if (!hasEnteredRef.current && mountOnEnter) {
    return null;
  }
  return children;
}
var NoopTransition_default = NoopTransition;

// node_modules/@restart/ui/esm/TabPanel.js
var import_jsx_runtime62 = __toModule(require_jsx_runtime());
var _excluded4 = ["active", "eventKey", "mountOnEnter", "transition", "unmountOnExit"];
var _excluded22 = ["activeKey", "getControlledId", "getControllerId"];
var _excluded32 = ["as"];
function _objectWithoutPropertiesLoose4(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
function useTabPanel(_ref) {
  let {
    active,
    eventKey,
    mountOnEnter,
    transition,
    unmountOnExit
  } = _ref, props = _objectWithoutPropertiesLoose4(_ref, _excluded4);
  const context4 = (0, import_react31.useContext)(TabContext_default);
  if (!context4)
    return [props, {
      eventKey,
      isActive: active,
      mountOnEnter,
      transition,
      unmountOnExit
    }];
  const {
    activeKey,
    getControlledId,
    getControllerId
  } = context4, rest = _objectWithoutPropertiesLoose4(context4, _excluded22);
  const key = makeEventKey(eventKey);
  return [Object.assign({}, props, {
    id: getControlledId(eventKey),
    "aria-labelledby": getControllerId(eventKey)
  }), {
    eventKey,
    isActive: active == null && key != null ? makeEventKey(activeKey) === key : active,
    transition: transition || rest.transition,
    mountOnEnter: mountOnEnter != null ? mountOnEnter : rest.mountOnEnter,
    unmountOnExit: unmountOnExit != null ? unmountOnExit : rest.unmountOnExit
  }];
}
var TabPanel = React56.forwardRef((_ref2, ref) => {
  let {
    as: Component2 = "div"
  } = _ref2, props = _objectWithoutPropertiesLoose4(_ref2, _excluded32);
  const [tabPanelProps, {
    isActive,
    onEnter,
    onEntering,
    onEntered,
    onExit,
    onExiting,
    onExited,
    mountOnEnter,
    unmountOnExit,
    transition: Transition = NoopTransition_default
  }] = useTabPanel(props);
  return (0, import_jsx_runtime62.jsx)(TabContext_default.Provider, {
    value: null,
    children: (0, import_jsx_runtime62.jsx)(SelectableContext_default.Provider, {
      value: null,
      children: (0, import_jsx_runtime62.jsx)(Transition, {
        in: isActive,
        onEnter,
        onEntering,
        onEntered,
        onExit,
        onExiting,
        onExited,
        mountOnEnter,
        unmountOnExit,
        children: (0, import_jsx_runtime62.jsx)(Component2, Object.assign({}, tabPanelProps, {
          ref,
          role: "tabpanel",
          hidden: !isActive,
          "aria-hidden": !isActive
        }))
      })
    })
  });
});
TabPanel.displayName = "TabPanel";
var TabPanel_default = TabPanel;

// node_modules/@restart/ui/esm/Tabs.js
var import_jsx_runtime63 = __toModule(require_jsx_runtime());
var Tabs = (props) => {
  const {
    id: userId,
    generateChildId: generateCustomChildId,
    onSelect: propsOnSelect,
    activeKey: propsActiveKey,
    defaultActiveKey,
    transition,
    mountOnEnter,
    unmountOnExit,
    children
  } = props;
  const [activeKey, onSelect] = useUncontrolledProp(propsActiveKey, defaultActiveKey, propsOnSelect);
  const id = useSSRSafeId(userId);
  const generateChildId = (0, import_react32.useMemo)(() => generateCustomChildId || ((key, type) => id ? `${id}-${type}-${key}` : null), [id, generateCustomChildId]);
  const tabContext = (0, import_react32.useMemo)(() => ({
    onSelect,
    activeKey,
    transition,
    mountOnEnter: mountOnEnter || false,
    unmountOnExit: unmountOnExit || false,
    getControlledId: (key) => generateChildId(key, "tabpane"),
    getControllerId: (key) => generateChildId(key, "tab")
  }), [onSelect, activeKey, transition, mountOnEnter, unmountOnExit, generateChildId]);
  return (0, import_jsx_runtime63.jsx)(TabContext_default.Provider, {
    value: tabContext,
    children: (0, import_jsx_runtime63.jsx)(SelectableContext_default.Provider, {
      value: onSelect || null,
      children
    })
  });
};
Tabs.Panel = TabPanel_default;
var Tabs_default = Tabs;

// node_modules/react-bootstrap/esm/getTabTransitionComponent.js
function getTabTransitionComponent(transition) {
  if (typeof transition === "boolean") {
    return transition ? Fade_default : void 0;
  }
  return transition;
}

// node_modules/react-bootstrap/esm/TabContainer.js
var import_jsx_runtime64 = __toModule(require_jsx_runtime());
var TabContainer = (_a) => {
  var _b = _a, {
    transition
  } = _b, props = __objRest(_b, [
    "transition"
  ]);
  return (0, import_jsx_runtime64.jsx)(Tabs_default, __spreadProps(__spreadValues({}, props), {
    transition: getTabTransitionComponent(transition)
  }));
};
TabContainer.displayName = "TabContainer";
var TabContainer_default = TabContainer;

// node_modules/react-bootstrap/esm/TabContent.js
var TabContent_default = createWithBsPrefix("tab-content");

// node_modules/react-bootstrap/esm/TabPane.js
var import_classnames41 = __toModule(require_classnames());
var React59 = __toModule(require_react());
var import_jsx_runtime65 = __toModule(require_jsx_runtime());
var TabPane = React59.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    transition
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "transition"
  ]);
  const [_a2, ..._b2] = useTabPanel(__spreadProps(__spreadValues({}, props), {
    transition: getTabTransitionComponent(transition)
  })), _c = _a2, {
    className,
    as: Component2 = "div"
  } = _c, rest = __objRest(_c, [
    "className",
    "as"
  ]), [{
    isActive,
    onEnter,
    onEntering,
    onEntered,
    onExit,
    onExiting,
    onExited,
    mountOnEnter,
    unmountOnExit,
    transition: Transition = NoopTransition_default
  }] = _b2;
  const prefix = useBootstrapPrefix(bsPrefix, "tab-pane");
  return (0, import_jsx_runtime65.jsx)(TabContext_default.Provider, {
    value: null,
    children: (0, import_jsx_runtime65.jsx)(SelectableContext_default.Provider, {
      value: null,
      children: (0, import_jsx_runtime65.jsx)(Transition, {
        in: isActive,
        onEnter,
        onEntering,
        onEntered,
        onExit,
        onExiting,
        onExited,
        mountOnEnter,
        unmountOnExit,
        children: (0, import_jsx_runtime65.jsx)(Component2, __spreadProps(__spreadValues({}, rest), {
          ref,
          className: (0, import_classnames41.default)(className, prefix, isActive && "active")
        }))
      })
    })
  });
});
TabPane.displayName = "TabPane";
var TabPane_default = TabPane;

// node_modules/react-bootstrap/esm/Tab.js
var import_jsx_runtime66 = __toModule(require_jsx_runtime());
var import_jsx_runtime67 = __toModule(require_jsx_runtime());
var propTypes4 = {
  eventKey: import_prop_types6.default.oneOfType([import_prop_types6.default.string, import_prop_types6.default.number]),
  title: import_prop_types6.default.node.isRequired,
  disabled: import_prop_types6.default.bool,
  tabClassName: import_prop_types6.default.string
};
var Tab = () => {
  throw new Error("ReactBootstrap: The `Tab` component is not meant to be rendered! It's an abstract component that is only valid as a direct Child of the `Tabs` Component. For custom tabs components use TabPane and TabsContainer directly");
  return (0, import_jsx_runtime67.jsx)(import_jsx_runtime66.Fragment, {});
};
Tab.propTypes = propTypes4;
var Tab_default = Object.assign(Tab, {
  Container: TabContainer_default,
  Content: TabContent_default,
  Pane: TabPane_default
});

// node_modules/react-bootstrap/esm/Table.js
var import_classnames42 = __toModule(require_classnames());
var React61 = __toModule(require_react());
var import_jsx_runtime68 = __toModule(require_jsx_runtime());
var Table = React61.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    striped,
    bordered,
    borderless,
    hover,
    size: size2,
    variant,
    responsive
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "striped",
    "bordered",
    "borderless",
    "hover",
    "size",
    "variant",
    "responsive"
  ]);
  const decoratedBsPrefix = useBootstrapPrefix(bsPrefix, "table");
  const classes = (0, import_classnames42.default)(className, decoratedBsPrefix, variant && `${decoratedBsPrefix}-${variant}`, size2 && `${decoratedBsPrefix}-${size2}`, striped && `${decoratedBsPrefix}-striped`, bordered && `${decoratedBsPrefix}-bordered`, borderless && `${decoratedBsPrefix}-borderless`, hover && `${decoratedBsPrefix}-hover`);
  const table = (0, import_jsx_runtime68.jsx)("table", __spreadProps(__spreadValues({}, props), {
    className: classes,
    ref
  }));
  if (responsive) {
    let responsiveClass = `${decoratedBsPrefix}-responsive`;
    if (typeof responsive === "string") {
      responsiveClass = `${responsiveClass}-${responsive}`;
    }
    return (0, import_jsx_runtime68.jsx)("div", {
      className: responsiveClass,
      children: table
    });
  }
  return table;
});
var Table_default = Table;

// node_modules/react-bootstrap/esm/Tabs.js
var React62 = __toModule(require_react());
var import_jsx_runtime69 = __toModule(require_jsx_runtime());
var import_jsx_runtime70 = __toModule(require_jsx_runtime());
var defaultProps24 = {
  variant: "tabs",
  mountOnEnter: false,
  unmountOnExit: false
};
function getDefaultActiveKey(children) {
  let defaultActiveKey;
  forEach(children, (child) => {
    if (defaultActiveKey == null) {
      defaultActiveKey = child.props.eventKey;
    }
  });
  return defaultActiveKey;
}
function renderTab(child) {
  const {
    title,
    eventKey,
    disabled,
    tabClassName,
    id
  } = child.props;
  if (title == null) {
    return null;
  }
  return (0, import_jsx_runtime69.jsx)(NavItem_default, {
    as: "li",
    role: "presentation",
    children: (0, import_jsx_runtime69.jsx)(NavLink_default, {
      as: "button",
      type: "button",
      eventKey,
      disabled,
      id,
      className: tabClassName,
      children: title
    })
  });
}
var Tabs2 = (props) => {
  const _a = useUncontrolled(props, {
    activeKey: "onSelect"
  }), {
    id,
    onSelect,
    transition,
    mountOnEnter,
    unmountOnExit,
    children,
    activeKey = getDefaultActiveKey(children)
  } = _a, controlledProps = __objRest(_a, [
    "id",
    "onSelect",
    "transition",
    "mountOnEnter",
    "unmountOnExit",
    "children",
    "activeKey"
  ]);
  return (0, import_jsx_runtime70.jsxs)(Tabs_default, {
    id,
    activeKey,
    onSelect,
    transition: getTabTransitionComponent(transition),
    mountOnEnter,
    unmountOnExit,
    children: [(0, import_jsx_runtime69.jsx)(Nav_default2, __spreadProps(__spreadValues({}, controlledProps), {
      role: "tablist",
      as: "ul",
      children: map(children, renderTab)
    })), (0, import_jsx_runtime69.jsx)(TabContent_default, {
      children: map(children, (child) => {
        const childProps = __spreadValues({}, child.props);
        delete childProps.title;
        delete childProps.disabled;
        delete childProps.tabClassName;
        return (0, import_jsx_runtime69.jsx)(TabPane_default, __spreadValues({}, childProps));
      })
    })]
  });
};
Tabs2.defaultProps = defaultProps24;
Tabs2.displayName = "Tabs";
var Tabs_default2 = Tabs2;

// node_modules/react-bootstrap/esm/Toast.js
var React66 = __toModule(require_react());
var import_react34 = __toModule(require_react());
var import_classnames44 = __toModule(require_classnames());

// node_modules/react-bootstrap/esm/ToastFade.js
var React63 = __toModule(require_react());
var import_jsx_runtime71 = __toModule(require_jsx_runtime());
var fadeStyles = {
  [ENTERING]: "showing",
  [EXITING]: "showing show"
};
var ToastFade = React63.forwardRef((props, ref) => (0, import_jsx_runtime71.jsx)(Fade_default, __spreadProps(__spreadValues({}, props), {
  ref,
  transitionClasses: fadeStyles
})));
ToastFade.displayName = "ToastFade";
var ToastFade_default = ToastFade;

// node_modules/react-bootstrap/esm/ToastHeader.js
var import_classnames43 = __toModule(require_classnames());
var React65 = __toModule(require_react());
var import_react33 = __toModule(require_react());

// node_modules/react-bootstrap/esm/ToastContext.js
var React64 = __toModule(require_react());
var ToastContext = React64.createContext({
  onClose() {
  }
});
var ToastContext_default = ToastContext;

// node_modules/react-bootstrap/esm/ToastHeader.js
var import_jsx_runtime72 = __toModule(require_jsx_runtime());
var import_jsx_runtime73 = __toModule(require_jsx_runtime());
var defaultProps25 = {
  closeLabel: "Close",
  closeButton: true
};
var ToastHeader = React65.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    closeLabel,
    closeVariant,
    closeButton,
    className,
    children
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "closeLabel",
    "closeVariant",
    "closeButton",
    "className",
    "children"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast-header");
  const context4 = (0, import_react33.useContext)(ToastContext_default);
  const handleClick = useEventCallback((e) => {
    context4 == null ? void 0 : context4.onClose == null ? void 0 : context4.onClose(e);
  });
  return (0, import_jsx_runtime73.jsxs)("div", __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames43.default)(bsPrefix, className),
    children: [children, closeButton && (0, import_jsx_runtime72.jsx)(CloseButton_default, {
      "aria-label": closeLabel,
      variant: closeVariant,
      onClick: handleClick,
      "data-dismiss": "toast"
    })]
  }));
});
ToastHeader.displayName = "ToastHeader";
ToastHeader.defaultProps = defaultProps25;
var ToastHeader_default = ToastHeader;

// node_modules/react-bootstrap/esm/ToastBody.js
var ToastBody_default = createWithBsPrefix("toast-body");

// node_modules/react-bootstrap/esm/Toast.js
var import_jsx_runtime74 = __toModule(require_jsx_runtime());
var Toast = React66.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    className,
    transition: Transition = ToastFade_default,
    show = true,
    animation = true,
    delay = 5e3,
    autohide = false,
    onClose,
    bg
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "className",
    "transition",
    "show",
    "animation",
    "delay",
    "autohide",
    "onClose",
    "bg"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast");
  const delayRef = (0, import_react34.useRef)(delay);
  const onCloseRef = (0, import_react34.useRef)(onClose);
  (0, import_react34.useEffect)(() => {
    delayRef.current = delay;
    onCloseRef.current = onClose;
  }, [delay, onClose]);
  const autohideTimeout = useTimeout();
  const autohideToast = !!(autohide && show);
  const autohideFunc = (0, import_react34.useCallback)(() => {
    if (autohideToast) {
      onCloseRef.current == null ? void 0 : onCloseRef.current();
    }
  }, [autohideToast]);
  (0, import_react34.useEffect)(() => {
    autohideTimeout.set(autohideFunc, delayRef.current);
  }, [autohideTimeout, autohideFunc]);
  const toastContext = (0, import_react34.useMemo)(() => ({
    onClose
  }), [onClose]);
  const hasAnimation = !!(Transition && animation);
  const toast = (0, import_jsx_runtime74.jsx)("div", __spreadProps(__spreadValues({}, props), {
    ref,
    className: (0, import_classnames44.default)(bsPrefix, className, bg && `bg-${bg}`, !hasAnimation && (show ? "show" : "hide")),
    role: "alert",
    "aria-live": "assertive",
    "aria-atomic": "true"
  }));
  return (0, import_jsx_runtime74.jsx)(ToastContext_default.Provider, {
    value: toastContext,
    children: hasAnimation && Transition ? (0, import_jsx_runtime74.jsx)(Transition, {
      in: show,
      unmountOnExit: true,
      children: toast
    }) : toast
  });
});
Toast.displayName = "Toast";
var Toast_default = Object.assign(Toast, {
  Body: ToastBody_default,
  Header: ToastHeader_default
});

// node_modules/react-bootstrap/esm/ToastContainer.js
var import_classnames45 = __toModule(require_classnames());
var React67 = __toModule(require_react());
var import_jsx_runtime75 = __toModule(require_jsx_runtime());
var positionClasses = {
  "top-start": "top-0 start-0",
  "top-center": "top-0 start-50 translate-middle-x",
  "top-end": "top-0 end-0",
  "middle-start": "top-50 start-0 translate-middle-y",
  "middle-center": "top-50 start-50 translate-middle",
  "middle-end": "top-50 end-0 translate-middle-y",
  "bottom-start": "bottom-0 start-0",
  "bottom-center": "bottom-0 start-50 translate-middle-x",
  "bottom-end": "bottom-0 end-0"
};
var ToastContainer = React67.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    position,
    className,
    as: Component2 = "div"
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "position",
    "className",
    "as"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast-container");
  return (0, import_jsx_runtime75.jsx)(Component2, __spreadProps(__spreadValues({
    ref
  }, props), {
    className: (0, import_classnames45.default)(bsPrefix, position && `position-absolute ${positionClasses[position]}`, className)
  }));
});
ToastContainer.displayName = "ToastContainer";
var ToastContainer_default = ToastContainer;

// node_modules/react-bootstrap/esm/ToggleButton.js
var import_classnames46 = __toModule(require_classnames());
var React68 = __toModule(require_react());
var import_jsx_runtime76 = __toModule(require_jsx_runtime());
var import_jsx_runtime77 = __toModule(require_jsx_runtime());
var import_jsx_runtime78 = __toModule(require_jsx_runtime());
var noop5 = () => void 0;
var ToggleButton = React68.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    name,
    className,
    checked,
    type,
    onChange,
    value,
    disabled,
    id,
    inputRef
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "name",
    "className",
    "checked",
    "type",
    "onChange",
    "value",
    "disabled",
    "id",
    "inputRef"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "btn-check");
  return (0, import_jsx_runtime78.jsxs)(import_jsx_runtime77.Fragment, {
    children: [(0, import_jsx_runtime76.jsx)("input", {
      className: bsPrefix,
      name,
      type,
      value,
      ref: inputRef,
      autoComplete: "off",
      checked: !!checked,
      disabled: !!disabled,
      onChange: onChange || noop5,
      id
    }), (0, import_jsx_runtime76.jsx)(Button_default2, __spreadProps(__spreadValues({}, props), {
      ref,
      className: (0, import_classnames46.default)(className, disabled && "disabled"),
      type: void 0,
      as: "label",
      htmlFor: id
    }))]
  });
});
ToggleButton.displayName = "ToggleButton";
var ToggleButton_default = ToggleButton;

// node_modules/react-bootstrap/esm/ToggleButtonGroup.js
var React69 = __toModule(require_react());
var import_invariant2 = __toModule(require_browser());
var import_jsx_runtime79 = __toModule(require_jsx_runtime());
var defaultProps26 = {
  type: "radio",
  vertical: false
};
var ToggleButtonGroup = React69.forwardRef((props, ref) => {
  const _a = useUncontrolled(props, {
    value: "onChange"
  }), {
    children,
    type,
    name,
    value,
    onChange
  } = _a, controlledProps = __objRest(_a, [
    "children",
    "type",
    "name",
    "value",
    "onChange"
  ]);
  const getValues = () => value == null ? [] : [].concat(value);
  const handleToggle = (inputVal, event) => {
    if (!onChange) {
      return;
    }
    const values = getValues();
    const isActive = values.indexOf(inputVal) !== -1;
    if (type === "radio") {
      if (!isActive && onChange)
        onChange(inputVal, event);
      return;
    }
    if (isActive) {
      onChange(values.filter((n) => n !== inputVal), event);
    } else {
      onChange([...values, inputVal], event);
    }
  };
  !(type !== "radio" || !!name) ? true ? (0, import_invariant2.default)(false, 'A `name` is required to group the toggle buttons when the `type` is set to "radio"') : (0, import_invariant2.default)(false) : void 0;
  return (0, import_jsx_runtime79.jsx)(ButtonGroup_default, __spreadProps(__spreadValues({}, controlledProps), {
    ref,
    children: map(children, (child) => {
      const values = getValues();
      const {
        value: childVal,
        onChange: childOnChange
      } = child.props;
      const handler = (e) => handleToggle(childVal, e);
      return React69.cloneElement(child, {
        type,
        name: child.name || name,
        checked: values.indexOf(childVal) !== -1,
        onChange: createChainedFunction_default(childOnChange, handler)
      });
    })
  }));
});
ToggleButtonGroup.defaultProps = defaultProps26;
var ToggleButtonGroup_default = Object.assign(ToggleButtonGroup, {
  Button: ToggleButton_default
});

// node_modules/react-bootstrap/esm/Tooltip.js
var import_classnames47 = __toModule(require_classnames());
var React70 = __toModule(require_react());
var import_jsx_runtime80 = __toModule(require_jsx_runtime());
var import_jsx_runtime81 = __toModule(require_jsx_runtime());
var defaultProps27 = {
  placement: "right"
};
var Tooltip = React70.forwardRef((_a, ref) => {
  var _b = _a, {
    bsPrefix,
    placement,
    className,
    style,
    children,
    arrowProps,
    popper: _,
    show: _2
  } = _b, props = __objRest(_b, [
    "bsPrefix",
    "placement",
    "className",
    "style",
    "children",
    "arrowProps",
    "popper",
    "show"
  ]);
  bsPrefix = useBootstrapPrefix(bsPrefix, "tooltip");
  const isRTL = useIsRTL();
  const [primaryPlacement] = (placement == null ? void 0 : placement.split("-")) || [];
  const bsDirection = getOverlayDirection(primaryPlacement, isRTL);
  return (0, import_jsx_runtime81.jsxs)("div", __spreadProps(__spreadValues({
    ref,
    style,
    role: "tooltip",
    "x-placement": primaryPlacement,
    className: (0, import_classnames47.default)(className, bsPrefix, `bs-tooltip-${bsDirection}`)
  }, props), {
    children: [(0, import_jsx_runtime80.jsx)("div", __spreadValues({
      className: "tooltip-arrow"
    }, arrowProps)), (0, import_jsx_runtime80.jsx)("div", {
      className: `${bsPrefix}-inner`,
      children
    })]
  }));
});
Tooltip.defaultProps = defaultProps27;
Tooltip.displayName = "Tooltip";
var Tooltip_default = Tooltip;
export {
  Accordion_default as Accordion,
  AccordionButton_default as AccordionButton,
  AccordionCollapse_default as AccordionCollapse,
  AccordionContext_default as AccordionContext,
  Alert_default as Alert,
  Anchor_default2 as Anchor,
  Badge_default as Badge,
  Breadcrumb_default as Breadcrumb,
  BreadcrumbItem_default as BreadcrumbItem,
  Button_default2 as Button,
  ButtonGroup_default as ButtonGroup,
  ButtonToolbar_default as ButtonToolbar,
  Card_default as Card,
  CardGroup_default as CardGroup,
  CardImg_default as CardImg,
  Carousel_default as Carousel,
  CarouselItem_default as CarouselItem,
  CloseButton_default as CloseButton,
  Col_default as Col,
  Collapse_default as Collapse,
  Container_default as Container,
  Dropdown_default2 as Dropdown,
  DropdownButton_default as DropdownButton,
  Fade_default as Fade,
  Figure_default as Figure,
  FloatingLabel_default as FloatingLabel,
  Form_default as Form,
  FormCheck_default as FormCheck,
  FormControl_default as FormControl,
  FormFloating_default as FormFloating,
  FormGroup_default as FormGroup,
  FormLabel_default as FormLabel,
  FormSelect_default as FormSelect,
  FormText_default as FormText,
  Image_default as Image,
  InputGroup_default as InputGroup,
  ListGroup_default as ListGroup,
  ListGroupItem_default as ListGroupItem,
  Modal_default2 as Modal,
  ModalBody_default as ModalBody,
  ModalDialog_default as ModalDialog,
  ModalFooter_default as ModalFooter,
  ModalTitle_default as ModalTitle,
  Nav_default2 as Nav,
  NavDropdown_default as NavDropdown,
  NavItem_default as NavItem,
  NavLink_default as NavLink,
  Navbar_default as Navbar,
  NavbarBrand_default as NavbarBrand,
  Offcanvas_default as Offcanvas,
  OffcanvasBody_default as OffcanvasBody,
  OffcanvasHeader_default as OffcanvasHeader,
  OffcanvasTitle_default as OffcanvasTitle,
  Overlay_default2 as Overlay,
  OverlayTrigger_default as OverlayTrigger,
  PageItem_default as PageItem,
  Pagination_default as Pagination,
  Placeholder_default as Placeholder,
  PlaceholderButton_default as PlaceholderButton,
  Popover_default as Popover,
  PopoverBody_default as PopoverBody,
  PopoverHeader_default as PopoverHeader,
  ProgressBar_default as ProgressBar,
  Ratio_default as Ratio,
  Row_default as Row,
  SSRProvider_default as SSRProvider,
  Spinner_default as Spinner,
  SplitButton_default as SplitButton,
  Stack_default as Stack,
  Tab_default as Tab,
  TabContainer_default as TabContainer,
  TabContent_default as TabContent,
  TabPane_default as TabPane,
  Table_default as Table,
  Tabs_default2 as Tabs,
  ThemeProvider_default as ThemeProvider,
  Toast_default as Toast,
  ToastBody_default as ToastBody,
  ToastContainer_default as ToastContainer,
  ToastHeader_default as ToastHeader,
  ToggleButton_default as ToggleButton,
  ToggleButtonGroup_default as ToggleButtonGroup,
  Tooltip_default as Tooltip,
  useAccordionButton
};
//# sourceMappingURL=react-bootstrap.js.map
